# Pibo documentation update log

## 2026-09-22

- **Shared observation engine extracted without changing source policies**: moved observation normalization, query/paging, regex, types and formatting into `agent-runtime/observations`; Delegation, Remote Agent and debug now consume the neutral owner. Code checkpoint `3b86d389` passed the targeted engine typecheck, rebuilt all 22 plugin artifacts, and passed 66 focused tests plus three separate characterization tests. The latter expose the existing timestamp-tie repeat/miss, shared-token session/query cursor, and full-normalization-before-limit behavior; no cursor or scalability fix is claimed. Updated [delegation](/specs/orchestration/subagents.md) and [Remote Agent](/specs/resources/remote-agent-plugin-contract.md) ownership and corrected stale yielded-only wording in the delegation contract and native glossary against the existing direct-send implementation. Archived path-redacted [command/output evidence](/reports/artifacts/beta4-phase2/direct-cutover-2026-09-22/evidence.json), including failed attempts and source/verification limits, so these results are not confined to ignored host workbench files. Candidate installation remains user-skipped; no production change.

- **Direct V4 implementation: Pi ownership and attachment registration**: removed the obsolete Core/Pi forwarding modules and runtime TUI entry, kept prompt persistence/recovery scheduling in Core, and moved Pi compaction/classification into the adapter. Core CLI discovery no longer loads the gateway graph. Added ownership-scoped backend/browser attachment-provider registration through the existing public plugin mechanisms; productive Composer/media/admission/receipt wiring is still open. Updated [runtime controls](/specs/runtime/provider-model-controls.md), [routing ownership](/specs/gateway/routing-events-and-actions.md), and the [public plugin contract](/specs/product/plugin-profile-catalog.md) to code checkpoints `09fed1a7` and `a1ccc8d9`, pushed on the isolated integration branch. Focused batches passed 70/70 and 55/55 (overlapping coverage, not a combined unique-test count); 22 plugin artifacts built and the targeted neutral/SDK typecheck passed. Fresh file-at-a-time behavioral emit is not a root typecheck or release compiler pass. The installation test is explicitly user-skipped, not green. No full-cutover completion, production deployment, data migration or publication is claimed.

## 2026-09-21

- **Reviewed wave-one packages integrated for candidate validation**: combined the A1/B1/C1/D1 branches in a separate integration worktree, added the attachment revision-exhaustion guard with a fail-first regression, and connected both transcription defaults to the reviewed owner-bound credentials. Registered the two package-local pilot READMEs as exact host-owned documentation exceptions; no validator rule was relaxed. Shared component and installation acceptance results are recorded separately from the original per-worker evidence.

- **Beta 4.0 same-DB fixture correction verified**: fixed the lifecycle regression to open the borrowed store on the same `pibo.sqlite` file the defaults use and added a negative lock control without injection (6/6 on the fix-commit modules, no production rebuild needed). Matrix restated as 358 executed tests across 63 unique verified files plus separate gateway CLI 1/5. Recorded in the [next-session handoff](/plans/beta4-phase2/next-session-handoff.md) and [build-gate closure report](/reports/beta4-phase2/beta4-build-gate-closure.md). No bundled full run is claimed.
- **Beta 4.0 readiness counter-check closed**: hardened the lifecycle regression (guaranteed teardown, stop handshake, AggregateError proof) as `716847b8`, added the three direct product-runtime regressions (31/31), and corrected the matrix count to 357 executed tests across 63 unique verified files. Recorded in the [next-session handoff](/plans/beta4-phase2/next-session-handoff.md) and [build-gate closure report](/reports/beta4-phase2/beta4-build-gate-closure.md). No bundled full run is claimed.
- **Beta 4.0 cutover gap closed at fix commit**: committed the shared-store injection with optional manifest declaration, the awaiting async remote disposer, and five real-host lifecycle regressions (fail-first proven against the void disposer) as `a6ef1a2f`; rebuilt from a pristine archive of that commit (exit 0) and verified 326 isolated tests plus a clean full typecheck against it, with gateway CLI at 1/5 as a documented timing finding. Recorded in the [next-session handoff](/plans/beta4-phase2/next-session-handoff.md) and [build-gate closure report](/reports/beta4-phase2/beta4-build-gate-closure.md). No bundled full run is claimed.
- **Beta 4.0 code checkpoint with fresh build and isolated matrix**: committed the 22-plugin composition gates, loader dist imports, and the composer send-owner guard with regression as `9b23ef3b` (14 files); rebuilt from a pristine archive of that commit (exit 0, 27 tarballs) and verified 320 isolated tests plus a clean full typecheck against it, with the cutover lock as a documented blocked path. Recorded in the [next-session handoff](/plans/beta4-phase2/next-session-handoff.md) and [build-gate closure report](/reports/beta4-phase2/beta4-build-gate-closure.md). No bundled full run is claimed.
- **Storage maintenance traceability rebound to a real checked commit**: `traceability.commit` in [storage maintenance](/specs/data/storage-maintenance.md) pointed at `730cf01f…`, which does not exist in this repository (verified missing object; origin of the stale hash unknown, presumably from another history). All seven evidence paths, eight symbols, and seven test names were verified present and the storage suites run 10/10, so the reference is now bound to the actually checked code commit `9b23ef3b`. Body and requirements unchanged; no validation rule weakened.

- **Beta 4.0 build-fix handoff and closure drafts published (uncommitted code state)**: added the draft [next-session handoff](/plans/beta4-phase2/next-session-handoff.md) and [build-gate closure report](/reports/beta4-phase2/beta4-build-gate-closure.md) with honest isolated-run evidence at HEAD `47342db6` plus uncommitted diff (composition files 15/15, remote 65/65, attachments 7/7, loops 95/95, ralph/UI 43/43 green; cutover gateway phase, tsx loader files, and gateway CLI timeouts red with documented classification; no bundled full run; no checkpoint commit yet). Registered both concepts in the migration ledger and regenerated their owning indexes. Docs-only change; no code, gate, timeout, or dependency change was made.
- **Beta 4.0 full-run status corrected**: the 20.09. bundled `npm test` log ends mid-run (4,424 lines, 2,990 pass / 23 fail to abort) with no summary, no exit code, and a gone process; the abort cause is unproven and no completion is claimed.

## 2026-09-20

- **Beta 4.0 core, plugin, bloat, and architecture audit published**: added the [Beta 4.0 audit](/reports/beta-4-0-core-plugin-bloat-audit-2026-09-20.md) with its [inventory appendix](/reports/beta-4-0-core-plugin-bloat-audit-2026-09-20-inventory.md), covering the verified `beta/4.0-plugin-system` baseline at `ece5f18c`, first-party inventory, 17 findings with file and line evidence, deletion and dependency matrices, a monorepo target architecture, a phased PR roadmap, a validation plan, and open questions. Read-only investigation only; no code, manifest, build, install, deployment, restart, or branch change was made.
- **Beta 4.0 audit correction pass**: reworded F-01 to a committed 21-vs-22 inconsistency without an executed-build claim (committed, worktree, and artifact layers separated), reframed F-05 per the binding F03/F06 five-core-views decision with headless core moved to user-decision scope, added F-17 with safe `--help`/`--version` startup timings (~500 ms core vs ~118 ms root shim), and made the zero-dependency-manifest caveat explicit. Evidence in appendix section G. Docs-only change; validation checks re-run.
- **Beta 4.0 audit review pass (9 ordered corrections)**: reworded F-01 to a statically proven gate contradiction without build claims, re-measured F-02 in retained bytes (≈59% Pi-/Provider-Code, `SettingsManager` corrected to type-only), downgraded F-03 to size/marker-consistent pending backend metafiles, removed `vscode-web` from deletion scope, reframed F-08 as owner/artifact mapping for the private build workspace, re-traced Ralph to an unreachable-stack deletion candidate, scoped F-10 to first-party backends, removed the "95 percent" claim, and embedded reproducible inventory filters in appendix section I. New report section 12 records corrections and limits. Docs-only change; validation checks re-run with stated results.
- **Beta 4.0 audit cross-check pass**: corrected F-14 against independent N1 (`src/ralph` unused island, active ralph surface on `loops`, existing DB and loop mode preserved), adopted canonical inventory 844/231878 gross and 819/224669 net from verified `.tmp/beta4-source-inventory.py`, delivered machine-readable `.tmp/beta4-retained-bundle-bytes.json` (167 package groups, Pi split, top-60 modules), removed all unmeasured savings projections as results, corrected `optionalDependencies`/dynamic-import mechanisms, fixed rollback to previous consistent assembly plus state snapshot, and added a precedence errata section 14. Docs-only change; validation checks re-run with stated results.
- **Beta 4.0 phase-2 documentation checkpoint**: archived the V3 work plan with HTML asset, four analysis assignments, 16 worker reports as non-approved research, the joint review (eight links explicitly retargeted to archive paths), the review checklist, the research acceptance with open points RV-01 to RV-08, the independent baseline review, the superseded plugin-session interaction draft (K07 is attachments-only), the codebase-design skill references, and a historical r1-draft record under `plans/beta4-phase2/`, `reports/beta4-phase2/`, and `legacy/beta4-phase2/`, plus a byte-identical originals ZIP with a source-to-target hash mapping under `reports/artifacts/beta4-phase2/`. Research and planning basis only; no implementation release, no normative spec, no retroactive r1 approval. Pre-existing strict failure `PIBO_TRACE_COMMIT` in `specs/data/storage-maintenance.md` left untouched and documented.
- **Beta 4.0 phase-2 checkpoint metadata fix**: corrected `generated.at` from the unsubstantiated `2026-09-20T17:30:00Z` (mislabeled local time, ahead of real UTC) to the actually captured `2026-09-20T15:54:34Z` in the 31 new checkpoint concepts. Bodies, originals, ZIP, and audit reports unchanged; hash provenance intact.
- **Beta 4.0 work plan V4**: published the V3 successor on baseline `84101adc` with remote impact in all packages plus derived V4 HTML; V3 concept metadata marked superseded with body and hashes unchanged.
- **Beta 4.0 remote contract docs**: added the draft remote-agent plugin contract spec with B-review limitations, entwurf Ist chapter 15 with corrected single-endpoint diagram, a baseline recount addendum, the historical source handoff, and a glossary entry.
- **Beta 4.0 controller-review follow-up**: incorporated F7 (session-plus-query cursors) and F1/F3 nuances into V4, spec, entwurf, and glossary; framed stale V3 audit statements as historical; archived the binding controller review; no blanket error-free or fully-green product claim.
- **Beta 4.0 foreign final-run attribution**: recorded the separate foreign `npm test` final run at `84101adc` (16:28–16:32Z, exit 1 at the pre-existing 22/21 composition gate, 0 tests) in V4 evidence instead of a blanket never-attempted claim; no own rerun, no gate changes.

## 2026-09-19

- **Muse-native settlement investigation strengthened by a confirmed `ps_605…` recurrence**: updated the [investigation report](/reports/muse-native-session-settlement-investigation-2026-09-19.md) and [Docker and deployment-pool validation plan](/plans/muse-native-session-settlement-docker-headpool-validation.md) with a later turn whose Pibo event tail stopped after a successful 232.6 ms tool, while native Muse continued to a committed final response and `terminal: completed`. The view head became unavailable 1,886 native records before terminal, Pibo retained a running queue row, and the persistent Muse server remained alive and idle. The documents now distinguish process liveness, native turn state, view projection, and Pibo settlement; treat earlier healthy turns and the later failed turn in the same session separately; and add the incident shape to T6, T7, T13, the decision tree, evidence bundle, and handoff requirements. No runtime fix, provider call, deployment, gateway mutation, merge, push, or pool lease is claimed.
- **Muse-native settlement incident consolidated and validation planned**: added the [investigation report](/reports/muse-native-session-settlement-investigation-2026-09-19.md), which separates established evidence from the unresolved host-projection versus SDK gap-fill question, identifies capacity exhaustion as a downstream amplifier, and distinguishes normal end-of-turn gate latency from the unverified ten-minute UI observation. Added the linked [Docker and deployment-pool validation plan](/plans/muse-native-session-settlement-docker-headpool-validation.md) with deterministic protocol injection, real-inference limits, deployment-pool soak, headful SSE/browser diagnosis, decision gates, evidence requirements, and cleanup. This is investigation and planned validation only; no runtime fix, provider call, deployment, gateway mutation, or pool lease is claimed.
- **Session sidebar room folders merged onto Beta**: the Chat Web session sidebar now renders Rooms as an expandable folder tree with nested Sessions, Shared Chat first, guide lines, full-bleed Session selection with a 3px accent bar, icon-only active-Room marking, per-Room agent menus, and cached foreign lists with a minimal loading spinner instead of skeletons. Verified by the 338-test Chat Web UI suite (333 passed, 5 pre-existing skips, 0 failures), the Chat UI typecheck, and live dev-gateway acceptance of expand/collapse, Session switching, Room switching, and scroll stability. Recorded in the [room folders design record](/project/design/session-sidebar-folders.md). Merged onto beta/4.0-plugin-system; no push, publication, or release occurred.
- **Muse sandbox control completed on Beta**: Added validated per-agent `runtimeOptions.sandbox` choices (`auto`, `enabled`, `disabled`), the `/sandbox` gateway/terminal command, runtime status rendering, and an idle-only Muse host replacement that resumes the same native Session and persists the explicit override. Replacement and same-host recovery re-deliver the current session MCP configuration, and an unsupported runtime reports without mutation. Full build, 84 focused Sandbox/Muse/Registry/UI tests, 46 post-hardening Muse/Resource tests, and authenticated headful Agent Designer plus slash-command checks passed in the isolated worker. Updates the [Muse adapter](/specs/runtime/muse-native-adapter.md) and [routing contract](/specs/gateway/routing-events-and-actions.md). No deployment, publication, push, release, or production gateway mutation occurred.
- **Muse wedged turn intake recovers via same-host re-sync**: after a failed manual `session/compact` (deterministically `InvalidTarget` once an auto-compaction boundary exists), the long-lived host rejected every later `turn/start` with `event id … conflicts with an existing event`, bricking the session although the durable log stayed intact (verified: copied session data resumes and turns fine on a fresh host). `prompt()` now matches that rejection signature, re-syncs the native session on the same host via `session/resume` with rollback, warns, and retries the prompt exactly once; all other rejections surface immediately. The compact ack status (`accepted`, or `noop` with a reason) is now surfaced in the compact result instead of blanket success. Covered by fail-once/no-retry/persistent-wedge/`noop` fake-host tests plus a same-host-resume validation against released `muse` 1.3.0. Updates the [Muse adapter](/specs/runtime/muse-native-adapter.md) specification. No controller mutation, push, publication, merge, or release occurred.
- **Muse turn timeout raised to a hard 30 minutes**: agentic workloads such as test suites and builds legitimately go quiet for tens of minutes, so `requestTimeoutMs` now defaults to 30 minutes with a 30-minute ceiling (was 2 minutes with a 10-minute ceiling). Updates the [Muse adapter](/specs/runtime/muse-native-adapter.md) specification. No controller mutation, push, publication, merge, or release occurred.
- **Target plan for timeout user nudge and snooze**: recorded in [Muse turn timeout user nudge and snooze](/plans/muse-turn-timeout-user-nudge-and-snooze.md). When a turn exhausts its idle budget, the user will be nudged in Chat with the hanging command, choose wait or abort, and snooze for re-decision via the existing `user_input` runtime-request mechanism. Plan only, no implementation. No controller mutation, push, publication, merge, or release occurred.
- **Muse turn timeout tolerates provider retry backoff**: first turns on fresh native sessions died at 120s of silence while the host was legitimately waiting out Meta 503/504 retry backoff (native `turn/retryScheduled` is not exposed to turn consumers). A silent window now probes host liveness via `approval/listPending`; a responsive host earns up to three windows while an unresponsive host still fails at the first, and the message reports total silence plus host responsiveness. Verified by responsive-host recovery and fast-dead fake-host tests plus a live first-turn success on the dev gateway. Updates the [Muse adapter](/specs/runtime/muse-native-adapter.md) specification. No controller mutation, push, publication, merge, or release occurred.
- **Boot-tolerant default plugin updates and admission takeover**: a drain-blocked default-plugin update used to throw during boot, crash the gateway once, then leave the plugin `retiring` and unregistered on every later boot (observed: `pibo.runtime-muse-native` missing after a restart with a stale reserved generation admission). Blocked updates are now deferred with a loud log while the prior revision stays active, `retiring` installations with a drain-interrupted update resume once blockers clear, and session rebind releases superseded own-session admissions. The live dev store was repaired (released orphan admission, completed the staged activation) and verified healthy. Covered by `test/plugin-system-drain-recovery.test.mjs` and noted in the [plugin operations guide](/project/guides/plugin-development-and-operations.md). No controller mutation, push, publication, merge, or release occurred.
- **Muse turn timeout made idle-based**: productive turns doing steady tool work were killed by a fixed whole-turn `requestTimeoutMs` budget (observed: a feature-implementation turn with 20+ tool calls failed at 120s). Streamed items and deltas now extend the budget; only silence trips it, still interrupting the native turn and failing explicitly with the same timeout classification. Verified by a steady-activity fake-host test plus the existing silence-timeout tests. Updates the [Muse adapter](/specs/runtime/muse-native-adapter.md) specification. No controller mutation, push, publication, merge, or release occurred.
- **Muse shell sandbox fallback documented**: the muse-native runtime resolves `muse serve` sandbox flags from config `sandbox` (`auto` by default); on Linux, `auto` degrades to `--disable-sandbox` with a warning diagnostic when bubblewrap is missing or non-functional, or when the muse executable lives inside the session workspace. Bubblewrap was installed on the dev host and verified functional. Live verification covered a real shell turn in a `/root`-workspace session plus the resolver matrix. Updates the [Muse adapter](/specs/runtime/muse-native-adapter.md) specification. No controller mutation, push, publication, merge, or release occurred.
- **Beta transcription, Muse reasoning, and Designer copy fixes documented**: Code candidate `ec7a4ff5c2c3d15542372395eed848c0df52b55d` keys packaged transcription contributions by provider ID so `POST /api/chat/transcription` resolves listed providers, derives Pi OAuth request auth from still-valid stored tokens when bundled backends cannot load pi's lazy auth modules, accepts `off` as `none` for mid-session Muse reasoning changes, and seeds Agent Designer profile copies with the default plugin selection so autosave persists them. Live dev-gateway verification covered a real ChatGPT round-trip, thinking show/set on a Muse session, and an agent copy save with cleanup. Updates the [Muse adapter](/specs/runtime/muse-native-adapter.md), [transcription and speech providers](/specs/resources/transcription-and-speech-providers.md), and [context, settings, and Agent Designer](/specs/web/context-settings-and-agent-designer.md) specifications. No controller mutation, push, publication, merge, or release occurred.

## 2026-09-16

- **Pibo 4 Beta optimistic send and Run Control accepted on Pibo2**: Candidate `699ff36b46f78444a59553c4b89d289f02722606` clears the composer in the optimistic send interaction, restores a failed message only while its transaction still owns the unchanged draft, and treats gateway RSS alone as a warning while retaining host-memory, heap, reservation, and concurrency admission blocks. Exact Assembly `7147c768…` runs on canonical Pibo2 after a checksum-bound stopped-boundary Cutover. Headful acceptance proved immediate clear, injected-failure restoration, Luna Medium Run Control delegation through `pibo_run_start`/wait/read, and preserved tool token/cache diagnostics. The [validation report](/reports/pibo-4-beta-optimistic-send-run-control-pibo2-acceptance-2026-09-16.md) records local tests, activation recovery, verified backups, and the remaining old pool-wrapper limitation.

- **Pibo 4 Beta Core delegation, Codex delivery, and scroll correction accepted on Pibo2**: Updated the [completion plan](/plans/pibo-4-0-plugin-completion.md), [task ledger](/plans/pibo-4-0-plugin-completion-todo.md), and [Pibo2 validation report](/reports/pibo-4-beta-core-delegation-pibo2-acceptance-2026-09-16.md). Profiles with effective Subagents receive all four Core-owned `pibo_agents_*` tools automatically and can delegate directly without Run Control; profiles without Subagents receive none. The retired Delegation package is absent from Plugin Management and Agent Designer while its persisted migration tombstone remains safe. Exact candidate `2f3159cb` and Assembly `53d311ac…` run on canonical Pibo2. Luna Medium read project files through the bundled `codex-code-mode-host` and completed a direct `helper` delegation; desktop/mobile Settings scrolling, Room/Session scroll retention, product tabs, tool views, and token/cache debug signals passed. The accepted code commit was verified on upstream Beta. No controller mutation, publication, merge into `dev` or `main`, or release occurred.

## 2026-09-15

- **Pibo 4 Beta user feedback accepted on Pibo2**: Candidate `f519fc78253b158abf3297d3bb2b8872ee557ff1` fixes Settings scrolling, Room return navigation, workspace tabs, native model error feedback, and OMP queue blocking. The exact Standard assembly passed the documented public/headful paths. Original Codex gpt-6-astra and OMP Sessions now answer and return to idle. The [acceptance report](/reports/beta-feedback-pibo2-2026-09-15.md) records 3,063 passing integrated tests, final package checks, checksum, preserved-state recovery, and outstanding operator workflow gaps.

- **Pibo 4 Beta feedback UI package completed locally**: Code candidate `c6e3943096bd45158393d59519b525b4365679c7` fixes viewport-bounded Settings scrolling and cache-backed, generation-guarded A→B→A Room switching. Preview and VS Code are ordinary plugin tabs; annotation-intent profiles expose the Web Annotations test UI without overriding explicit disables; packaged browser views share the host React Query instance. Standard now contains 21 plugin packages and the Candidate contains 24 tarballs. Root build/typecheck, focused UI/plugin/package/Cutover tests, artifact generation, and authenticated headful desktop/mobile flows passed in the isolated worker. The [validation report](/reports/beta-feedback-ui-2026-09-15.md) records evidence and the prepared Pibo2 test-profile recipe. No Pibo2/controller mutation, deployment, publication, push, PR, merge, or release occurred.

- **Pibo 4 canonical Pibo2 acceptance completed**: The exact content-addressed candidate from commit `cb975d3e91aec82763990ab76b0626d23bd8f2d9`, SHA-256 `b5f5219b8958584902afdb13c9f6dd3eb889a3c1e9aae8cfe9b73ee5f2ba1f38`, migrated the retained 1.7.2 installation to 20 active independent plugins and five uninstalled aggregate-owner tombstones. Four representative existing Sessions retained profile, room, workspace, history, and runtime binding. An old Pi Session and an old Codex Native Session completed real model turns; Pi completed another turn after restart. The completion receipt remained byte-identical across the canonical restart. Authenticated headful Web acceptance showed retained history, Core Settings, and Plugin management without browser-console errors. The [Pibo2 acceptance report](/reports/pibo-4-0-plugin-system-pibo2-acceptance-2026-09-15.md) records the evidence. No controller mutation, npm publication, PR, merge, or release occurred.

- **N-033 runtime/resource delivery reclosed after staging evidence**: Code candidate `cb975d3e91aec82763990ab76b0626d23bd8f2d9` retains the packed Pi OAuth correction and closes the remaining User Resource path defect without a Core runtime exception. The Pi plugin still embeds every lazy OAuth loader for both bundled `pi-ai` copies, and a clean offline consumer derives retained OpenAI-Codex OAuth through the installed backend. Real staging then exposed a retained absolute `maintain-okf-docs` path under the former Pibo Home. Direct `PIBO_HOME` authority now rebases such paths generically to `${PIBO_HOME}/user-skills/<name>/SKILL.md` when that managed resource exists; explicitly configured other roots retain their previous behavior. The User Resource remains external to Core and Standard, and all four saved profile choices remain intact. The packed Standard regression starts from the stale retained path, confirms the rebased bootstrap catalog, enters real Gateway turn preparation, and always terminates its process group. Focused coverage passed 34/34; broader relevant coverage passed 271/272 with one expected skip; separate Gateway integration passed 5/5. The rebuilt 23-artifact Candidate is 38,364,230 bytes with SHA-256 `b5f5219b8958584902afdb13c9f6dd3eb889a3c1e9aae8cfe9b73ee5f2ba1f38`. No Pibo2/controller mutation, push, publication, merge, release or deployment occurred.

- **N-032 automatic SDK handoff closed**: Code candidate `b785ca10d28bbbfcbe06b2efb836a1e8defd2b79` preserves ordinary `preparePluginSdkResolution()` ownership refusal while adding a verified-Cutover-only call before the first `host.start`. It directly requires an idle `PluginHost`, refuses non-symlink paths, creates a temporary exact-target symlink and atomically renames it over the retained old owner. Retry with the same target is idempotent; active composition attempts leave the old link untouched. Standard now imports its executable Core from the exact checksum-bound `coreRoot`, avoiding divergent top-level versus bundled package ownership. The packed Candidate regression begins with an Artifact-Root SDK symlink to a different retained old package, completes all 20 target mappings without Session/Chat/plugin loss, and reuses the new link across restart. Gateway test cleanup now escalates from SIGTERM to SIGKILL and destroys child pipes so assertion failures cannot strand `node --test`. Focused coverage passed 52/53 with one expected skip; broader Plugin-System/Cutover/package coverage passed 242/243 with one expected skip; Gateway integration passed 5/5. No Pibo2/controller mutation, push, publication, merge, release or deployment occurred.

- **N-031 cold revision cutover closed**: Code candidate `9a23906fc012f7d484cbaf3b464073867f561679` confirms the root cause: normal `PluginManager.activate()` may legitimately return a nonterminal drain state, while the old Cutover loop neither required `complete` nor provided the full replacement dependency graph. Verified Cutover now stages all active revisions first and invokes a separate caller-transactional cold batch lifecycle; ordinary live activation/drain behavior is unchanged. Cold blockers are limited to reserved generation admissions, active runs/runtimes and active/required dependent plugins. Retained historical/optional/unknown Session/Profile consumers and 54 released admissions do not block. The realistic old `pibo.agent-delegation` revision test proves zero mutation/no receipt with a live blocker, then successful replacement and revision-stable restart. Packed Standard replaces all 20 old package revisions in one valid host graph while preserving 17 active, two disabled and one uninstalled selections. Broader Plugin-System/Cutover coverage passed 242/243 with one expected skip; Gateway integration passed 5/5. Rebuilt Candidate: 38,294,996 bytes, SHA-256 `cdacdabb25807c27efe96693357e347867c82b201ebf6d7a0b852bd8f3d4a562`. No Pibo2/controller mutation, push, publication, merge or deployment occurred.

- **N-030 superseded provider cutover conflict closed**: Code candidate `2ca6701f24125c5f7db0f6a2ffcfeeddf1d06275` adds a generic verified-migration exclusion to `PluginManager`: only IDs signed in the loaded plan's `supersededOwners` are omitted from import-free dependency/service resolution during Cutover activation. Normal starts and every non-superseded provider retain the strict conflict rule. Product Runtime holds all Cutover installation, operation and artifact-row changes in one SQLite transaction until successful activation; a later strict provider conflict rolls back to the exact old installation set and writes no receipt, while corrected replay and restart remain idempotent. The realistic fixture gives legacy `pibo.user-resources` the real `pibo.user-resources.service`, makes a target require it, then proves both successful Core resolution and rollback on a later non-superseded duplicate provider. Packed Standard coverage uses the same legacy provider with all 20 real plugins. Build passed; focused tests passed 65/66 with one expected skip, broader Plugin-System/Cutover coverage passed 231/232 with one expected skip, and isolated Gateway integration passed 5/5. Rebuilt Candidate: 38,290,357 bytes, SHA-256 `909aeae3d1e620c0d3879d7b6b64a572e9b88dda077871487208956c581a86d0`. No Pibo2/controller mutation, push, publication, merge or deployment occurred.

- **N-029 legacy Standard shell cutover closed**: Code candidate `eda0911e6bb7722f59f6c203e596f9cc40390f8a` adds the smallest one-time external Cutover mapping for the real `pibo.standard-shell` snapshot owner. It is represented by the `@pasko70/pibo-standard` composition, produces no plugin target and requires no fake artifact. Preparation records it in `supersededOwners`; the existing generic activation path tombstones it with the other legacy aggregates. The packed `1.7.2` Standard CLI/Gateway cutover test proves the owner is tombstoned while exactly 20 actual Standard plugin installations retain 17 active, two disabled and one uninstalled state across restart. Build passed; focused tests passed 15/16 with one expected optional old-package skip. The rebuilt Candidate is 38,289,654 bytes with SHA-256 `8663d0682416c9ae2749128bfa7398465d9f7fef63bd54b74440c8599f58089a`; bundled Cutover SHA-256 is `a23a4555b6b53930b3f7ad3dd79a91c988cb234414ac8ec1285a4460cfb08f8a`. No Core/fake-plugin delivery, Pibo2/controller mutation, push, publication, merge or deployment occurred.

- **N-028 real pre-4 source acceptance corrected**: Code candidate `34691ecf95fb9bcfdcde7d896537c9fa1dc1ea9f` makes the one-time external `@pasko70/pibo-cutover` prepare runner accept strict semantic 1.x, 2.x and 3.x package versions in addition to 4.0.0 alpha/beta/rc prereleases. This covers the real deployed `@pasko70/pibo@1.7.2` source while preserving exact package identity and checksum verification. Malformed versions, 0.x, 4.x stable/newer, unrelated 4.x prereleases and 5.x remain rejected. Core and Standard runtime behavior are unchanged. Full build passed; focused packed Cutover/Standard/Core tests passed 14/15 with one expected optional 3.6.2-tarball skip. The rebuilt 23-artifact Candidate is 38,289,617 bytes with SHA-256 `b4e4a87b77c6eaf296bde09c4089bf7ee4963380439372ca7ed664f72e6f3509`; its Cutover tarball SHA-256 is `d16289e0d3b752f94e3fbfcaec5bb93db9edcae459be062c46c0500ee7106acf`. No Pibo2/controller mutation, push, publication, merge, release or deployment occurred.

- **N-027 packed Standard cutover delivery closed**: Code candidate `bb010a72141ea32059e992c8e9fad54fe111bd3e` adds a content-addressed local Candidate assembly containing Core, `@pasko70/pibo-cutover`, Standard and exactly 20 plugin tarballs. Its manifest binds all 23 artifacts by bytes and SHA-256 and publishes the exact prepare, fresh-start and cutover-start commands. Standard consumes the public generic Core `--cutover-plan` contract, resolves target bytes from the installed assembly while the retained old source tarball remains external, materializes active/disabled/uninstalled selections before bootstrap, tombstones superseded built-in aggregate owners, writes a complete receipt and repeats without revision drift. The real offline packed CLI/Gateway test retained a legacy aggregate DB, Session and Chat message across cutover and restart. Focused delivery tests passed 38/39 with one expected old-package skip; the final serial suite passed 3,037 of 3,047 with 10 skips and no failures; isolated Gateway integration passed 5/5 without residue. Final assembly: 38,289,291 bytes, SHA-256 `359a5a2d4f1b6e31b0d02c4914a645ec2aaf354c1f9f57994030c5e82616d6c4`. No Pibo2/controller mutation, push, publication, merge, release or deployment occurred.

- **Pibo 4 executable Standard and Core skill-boundary correction**: Closed N-026 for code/package candidate `51bcfcef4653328823e79a0f2386b786fcf003d9`. `@pasko70/pibo-standard@4.0.0-beta.1` now provides `pibo` and `pibo-standard`, bundles Core plus its exact 20 package dependencies, installs as one offline tarball, and activates all 20 plugins on a fresh start and restart through the generic composition bootstrap. Deployment-Pool Candidates now install and launch Standard rather than the plugin-free Core. Minimal Core reports no extension skills; Standard skills belong to `pibo.builtin-profiles`, Web Annotations keeps its package owner, and every reported packaged skill path exists. User Resources now expose their Core service before plugin setup but materialize user contributions afterward, preventing `skill-creator` ownership conflicts. Chat signal projection now compares an immediate error signal with the durable last-read timestamp, removing the `idle`/`error` race without weakening its regression. Current Core and Standard tarball SHA-256 values are `fa617086c90459beb756ae9c43e851d61653d8fb51eaf592940a4e738795f8fb` and `20a931a2c84cd879217715d1a3e052abe9f2699918f19b277303c32559bf73ae`. Package tests passed 10/10, focused root-cause suites 31/31, the serial suite passed 3,036 of 3,046 tests with 10 skips and no failures, and isolated Gateway integration passed 5/5 without a residual test process. Headful Mobile 390×844 and Desktop 1280×800 showed all 20 plugins as active; CDP reloads had no failed responses or console errors. No publication, push, PR, merge, release, deployment, Pibo2 run, real-provider call, or controller-gateway mutation occurred.

- **Pibo 4 executable Minimal-Core delivery completed**: Closed N-025 and re-closed F06/F10 for code/package candidate `8ad776f1`. Generated `@pasko70/pibo@4.0.0-beta.1` now contains `bin/pibo`, `dist/bin/pibo.js`, `gateway:web`, Chat Web, five Core views, and bundled storage/telemetry workers. Its dedicated `core` profile starts without installed runtimes or plugins through `pibo.runtime-unassigned`; bootstrap, Session plugin-plan, and Agent plugin-preview return successful empty diagnostic results rather than inventing Pi. The final Core tarball SHA-256 is `e91fb9a8c66d4d8cd3bfb52efbab4652b325d613fb4ece04e07eacdb7133574a`. Executable-Core tests passed 5/5; the combined package/release/cutover/Candidate/pool group passed 28 tests with one expected skip; post-fix Runtime/Web tests passed 168/168; separate Gateway integration passed 5/5 without a residual test process. Headful Desktop 1280×800 showed all five Core tabs, and Mobile 390×844 showed Settings plus empty Plugin management; final CDP reloads had no network or console failures. No publication, push, PR, merge, release, deployment, Pibo2 run, real all-runtime provider call, or controller-gateway mutation occurred.

- **Pibo 4 local completion acceptance (superseded by N-025 for delivery)**: Closed F10 for code/package candidate `b30a1e03`, after the split-release correction `746b990c` and documentation reconciliation `0a1a45ed`. The candidate builds `@pasko70/pibo`, Cutover, and Standard at `4.0.0-beta.1` plus 20 independently packed plugins at `1.0.0`; Standard pins those exact versions. Six bounded serial regression groups excluding the separately constrained gateway test completed with 3,040 tests, 3,030 passes, no failures, and 10 skips; gateway integration passed 5/5 and exited without a remaining test process. Final typecheck, stepwise build, Pibo4 package build, strict OKF validation, 86 documentation tests, and current headful Desktop/Mobile Session-workspace checks passed. Controlled runtime fixtures are not relabeled as real-provider or Pibo2 evidence. No npm publication, push, PR, merge, release, deployment, Pibo2 run, or controller-gateway mutation occurred.

- **Pibo 4 npm release boundary correction**: Resolved the F10 packaging review at commit `746b990c`. The repository root is now a private build workspace rather than npm delivery evidence. The release wrapper versions, verifies, and publishes only generated Minimal-Core, Cutover, 20 independent plugin packages, and Standard; its hermetic test records every publish target and rejects a naked root publish. Replaced the misleading Root-pack assertions with actual Minimal-Core tarball/import checks and added separate Standard/Cutover packing evidence. Fourteen focused package/release tests passed locally in the isolated Docker worker; no npm publication or release was performed.

- **Pibo 4 plugin delivery reconciliation**: Replaced the retired Registry contract with the current capability-host, immutable package, profile-selection, public SDK, safe lifecycle, prepared-cutover, and no-legacy-delivery specification at commit `d37dea0c`. Updated App Context, Web host, Loop, transcription, and speech ownership references. Added the [plugin development and operations guide](/project/guides/plugin-development-and-operations.md) and an independently buildable Hello Pibo example covering public imports, inspection, activation, update, diagnosis, configuration, and uninstall.

## 2026-09-14

- **4.0 packed cutover and owner migration**: Completed F06-06 and F07 in the [completion plan](/plans/pibo-4-0-plugin-completion.md) and [todo](/plans/pibo-4-0-plugin-completion-todo.md). A separately packed cutover tool now records the complete legacy selection snapshot and exact source/Core/plugin tarball hashes before Minimal-Core replaces the monolith. Required-cutover startup rejects missing, changed, wrong-version, default-enabling, or conflicting targets before normal activation; it installs only mapped active artifacts and writes an idempotent completion receipt. The actual packed `@pasko70/pibo@3.6.2` path upgraded to packed `4.0.0-beta.1` with Preview and Workflows active while disabled Cron/Goal Loops and uninstalled Web Search stayed absent. Browser-v1 import now translates old Product UI targets to Core or split-plugin owners without inferring Session ownership. Root emit, SDK/package rebuilds, and 83 cutover/migration/selection/context/tab tests passed. Evidence: `/tmp/pibo4-f07-cutover.md` and `/tmp/pibo4-f07-tests.log`.

- **4.0 physical package boundaries**: Completed F06 except the intentionally coupled F06-06 old-monolith cutover. Added reproducible builders for a dependency-free Minimal-Core tarball surface, the exact-version Standard composition, and 20 independently packable first-party artifacts with self-contained backend and split browser code. Owner-specific setup modules prevent aggregate tool/control/transcription imports from widening unrelated artifacts. A clean consumer installed only the packed Minimal-Core, started with zero plugins, then installed and activated packed Preview; all 20 feature artifacts also completed independent `npm pack`. The Minimal-Core executable scan excludes concrete Pi/Codex/OMP, Run, Delegation, reminder, and browser-controller symbols. Evidence: `/tmp/pibo4-f06-package-boundaries.md`; `test/pibo4-packed-distribution.test.mjs` passed 4/4. The packed old-to-new upgrade remains F06-06/F07 work.

- **4.0 Runtime Requests extraction**: Advanced F05 in the [completion plan](/plans/pibo-4-0-plugin-completion.md) and [todo](/plans/pibo-4-0-plugin-completion-todo.md). Pi, Codex Native, and OMP now use separate setup modules. Codex Native owns approval/input response actions, runtime eligibility, workspace view, and browser entry; Runtime Requests are no longer a fixed Core session tool, while the inline Chat path keeps the same generic SSE/Pending queue. Debug resolves history providers from enabled installed runtime packages instead of statically importing all adapters. Root emit, Chat UI typecheck/build, and 75 focused tests passed. F05 remains partial until legacy Pi compatibility imports and root exports are removed with F06/F08 and recovery parity is rerun.

- **4.0 feature package split**: Completed F04 in the [completion plan](/plans/pibo-4-0-plugin-completion.md) and [todo](/plans/pibo-4-0-plugin-completion-todo.md). Preview, Cron, and Workflows now have separate backend modules; all first-party feature views use separate browser entries. Manifest `chatRoute` and `subviewNavigation` metadata replace Product UI IDs and the first-party navigation allowlist in normal host dispatch. Preview and Web Annotations are no longer Core session-tool values, missing feature routes retain state with an actionable reinstall empty state, and mutable feature entries bypass retained caches. Root emit, Chat UI typecheck/build, and 39 focused package/UI/cache tests passed. Self-contained npm artifacts, packed Minimal-Core closure, legacy-ID cutover, and integrated headful acceptance remain F06–F10 work.

- **4.0 Run/Delegation ownership inversion**: Completed F04-07 and the source-level portion of N-022 in the [completion plan](/plans/pibo-4-0-plugin-completion.md) and [todo](/plans/pibo-4-0-plugin-completion-todo.md). Run and Delegation packages now construct their own controllers; Delegation owns its tool name, child metadata, agent observation projection, and management presentation; Run Control owns reminder rendering and recognition. Core exposes only generic yielded-run and child-session orchestration services. Removed the obsolete Portable Tool controller-injection surface including `subagentRunner`. Root emit and 56 focused tests passed; the packed Minimal-Core import/content proof remains F06-07.

- **4.0 Core views**: Completed F03 in the [completion todo](/plans/pibo-4-0-plugin-completion-todo.md). Settings, Agent Designer, Context, Raw Events, and Session Inspector now use fixed Core registrations and the existing Session-owned tab lifecycle instead of Product UI view contributions. Core also owns auth, base Web, Chat, and user resources without installations; transitional Product UI/Web Product packages retain only feature surfaces pending F04. Root emit, Chat UI typecheck/build, 34 focused tests, and headful desktop/mobile checks passed. F02 remains partially open under N-022.

- **4.0 first-party tool extraction review**: The provider, selection, direct/yielded delivery, prompt transformation, cleanup, and dependency-expansion portions of F02 are implemented and passed Root emit, SDK build, Chat UI typecheck, and 167 focused tests. The review reopened F02-02 as N-022: moving Run and Delegation code into domain files did not establish package ownership because `core/session-router` still imports their concrete factories, the Delegation tool name, and Run reminder formatting. F04 must make each plugin construct/register its controller through generic orchestration/lifecycle services; F06 must prove the packed Minimal-Core contains no such feature closure. F03 continues independently.

- **4.0 public host/runtime contracts**: Completed F00 source/package boundary decisions and implemented F01 core-service bootstrap without a synthetic management-plugin installation. Added explicit backend, browser, and runtime SDK contracts; generation-pinned session tool providers whose tools remain individually declared, selected, runtime-filtered, schema-checked, and PluginContext-bound; and awaited, failure-visible provider cleanup. An out-of-repository TypeScript fixture using only public package subpaths compiled, installed, and ran. Full typecheck, backend/SDK build, and 42 focused tests passed. Packed no-symlink distribution and monolith-to-Minimal-Core cutover remain F06/F07 work.

- **4.0 implementation tracking**: Recorded full implementation authorization and created the [living completion todo list](/plans/pibo-4-0-plugin-completion-todo.md) from F00–F10, with stable task IDs, newly discovered work, and separate implementation/validation/acceptance states.

- **4.0 completion plan**: Added the [core and plugin completion plan](/plans/pibo-4-0-plugin-completion.md) with the owner-defined core views, independently deliverable feature/runtime packages, public service and tool contracts, minimal distribution, Codex Request ownership investigation, OMP maintenance-only scope, versioned 3.6.2/Beta migration, legacy API removal, preserved behavior tests, and documentation reconciliation. Linked precedence from the earlier plan and execution ledger. This change plans future behavior only; no implementation or deployment is claimed.

- **Container-responsive first-party tab navigation**: Added consistent left sidebar/local drawer behavior for Settings, User Resources/Context, Web Annotations, and first-party tool-family sections; retained the existing Agent Designer, Cron, and Loops pattern; made the nested Context Files panel observe its own container; and documented Workflows and Build Context as intentionally navigation-free. The host suppresses duplicate top subview navigation only for an explicit built-in list, preserving third-party layout autonomy. Ten scoped Docker tests, Chat UI typecheck/build, and authenticated wide/narrow/mobile Chromium checks passed. Two exploratory stale assertions reproduced at the starting commit and remain test debt. No gateway restart/configuration, Pibo2, deployment, push, PR, release, or full suite occurred.

- **Native-first Session recovery**: Implemented and documented adapter-owned binding rechecks, exact Pi/Codex stale-binding repair, durable same-runtime reconstruction only after authoritative absence, retry/CAS-safe provenance checkpoints, and fail-closed handling for auth, permissions, corruption, ambiguity, transient failures, insufficient history and unsupported adapters. OMP no longer silently keeps a fresh transcript after failed resume. Source commit `bcb36ccd`; focused Docker checks passed without Pibo2 access, push, PR, deployment, release, browser/model execution, controller-gateway mutation or full suite.

- Installed candidate `f01d453d` and verified delayed desktop/mobile Session switches against empty targets; recorded exact package identity and persisted-state evidence in the [execution ledger](/plans/unified-plugin-system-execution.md#pibo2-tab-ownership-correction-acceptance-2026-09-14).


- **Session tab carryover correction**: Updated the [Session workspace specification](/specs/web/session-workspace-lifecycle.md), [PLG-UX-002 plan](/plans/unified-plugin-system-rebuild.md#plg-ux-002-session-erstellung-und-session-eigener-workspace-lifecycle), and [execution ledger](/plans/unified-plugin-system-execution.md#follow-up-stale-route-carryover-on-session-switch-2026-09-14) for commit `e901ebcc`. Route reconciliation is now owned by the Session-selection generation that observed the route, so Session A's active tab cannot seed ready-empty or delayed-loading B/C/D during fast or delayed desktop/mobile switching. The focused regression failed before the fix and passed after it; 58 focused tests, Chat UI typecheck/build, and authenticated headful worker evidence passed. The current fix was not packaged or deployed to Pibo2, and no full suite, model turn, push, PR, release, or controller-gateway mutation was performed.

- **Pibo2 workspace evidence correction**: Corrected the [September 13 workspace record](/plans/unified-plugin-system-execution.md#pibo2-session-workspace-acceptance-2026-09-13): `/tmp/pibo2-workspace-accept.json` showed the other Session with a carried-over Settings tab, so that run did not prove per-Session empty-workspace isolation. Its panel-cache and targeted-Refresh observations remain historical; the carryover fix currently has worker-only evidence.

## 2026-09-13

- **Session workspace Pibo2 acceptance**: Installed candidate `03429d4f` and recorded authenticated headful desktop/mobile confirmation before create completion, draft handoff, Settings cache and targeted Refresh in the [execution ledger](/plans/unified-plugin-system-execution.md#pibo2-session-workspace-acceptance-2026-09-13). Later review of the same artifact found a carried-over Settings tab in the other Session, so the original per-Session empty-workspace claim is superseded by the September 14 correction; no model turn or release was performed.

- **Immediate optimistic Session rename and owned workspace lifecycle**: Consolidated the implemented [Session creation and workspace contract](/specs/web/session-workspace-lifecycle.md), aligned [PLG-UX-002](/plans/unified-plugin-system-rebuild.md#plg-ux-002-session-erstellung-und-session-eigener-workspace-lifecycle) and the [execution ledger](/plans/unified-plugin-system-execution.md#active-correction-session-owned-workspace-ux-2026-09-13), and recorded commit-backed focused tests, Chat UI build/typecheck, desktop/mobile delayed-POST evidence, and the delayed-PATCH selection race. An earlier headful Agent Designer run confirms the shared callback, while exact-candidate delayed-POST timing through that button remains an evidence limit; no full suite, Pibo2 deployment, push, PR, or release is claimed.

- **Pibo 4.0 corrected Pibo2 test candidate**: Recorded exact candidate/checksum, automatic profile migration parity, byte-identical user resources, valid Pi/Codex session plans and headful mobile Designer evidence in the [execution ledger](/plans/unified-plugin-system-execution.md#pibo2-upgrade-finding-and-local-correction). New remote model turns and release acceptance remain unverified.

- **Managed-package startup upgrade**: Recorded the real Pibo2 manifest/backend mismatch, restoration of the previous candidate, and corrected startup ordering with 12 passing targeted regressions in the [execution ledger](/plans/unified-plugin-system-execution.md#pibo2-upgrade-finding-and-local-correction).

- **Pibo 4.0 integration review**: Recorded verified historical v1 migration-journal recovery and retryable authenticated browser import in the [execution ledger](/plans/unified-plugin-system-execution.md#orchestrator-integration-review-2026-09-13), with 33 passing targeted tests and the current clean staged Docker build. Pibo2 upgrade acceptance remains pending.

- **Pibo 4.0 automatic upgrade correction**: Corrected the [execution ledger](/plans/unified-plugin-system-execution.md#active-direction-pibo-40-clean-migration-2026-09-13) after parent review found that old Session databases and browser-v1 tabs still required manual import. Gateway startup now detects and journal-imports a retained `pibo-sessions.sqlite` into `pibo.sqlite`; first Chat UI load snapshots and server-backs up `desktopTabs.v1`, then imports only explicitly Session-bound tabs through CAS. Unbound or conflicting records remain preserved with precise repair instructions. The post-correction focused set passes 35/35; the earlier 113-test result is no longer described as current-candidate aggregate evidence.

- **Pibo 4.0 plugin-only local cutover**: Updated the [execution ledger](/plans/unified-plugin-system-execution.md#active-direction-pibo-40-clean-migration-2026-09-13) with ordinary User-Resource ownership, removal of executable compatibility constructors/default registries, direct Debug adapter definitions, retryable and partial Agent migration, preserved conflict data, Core-skill ownership transition, real `SIGKILL` owner-write recovery, unavailable-runtime isolation, historical configuration/tabset and old session-store audit, 113 focused passing tests, clean stepwise Docker build, and mobile checkbox/autosave evidence. Current Fullsuite, real-provider Pibo2 execution and commit-backed specifications remain open.

- **Pibo 4.0 migration decision**: Added [PLG-V4-001](/plans/unified-plugin-system-rebuild.md#plg-v4-001-pibo-40-mit-automatischer-datenmigration): automatic preserved-data migration and one plugin delivery path, with isolated upgrade readers instead of parallel legacy execution; aligned the execution ledger and retained existing-test parity requirements.

- **Plugin philosophy and Designer feedback**: Revised the [rebuild plan](/plans/unified-plugin-system-rebuild.md#plg-ux-001-plugin-philosophie-optionale-module-und-getrennte-zuständigkeiten) and [execution ledger](/plans/unified-plugin-system-execution.md) to make workspace modules optional, route plugin configuration through Settings → Plugins, distinguish system ownership from selectable agent contributions through generic metadata, require collapsed categorized Designer controls and an actionable legacy migration. Planned behavior only; fast headful feedback iterations preserve existing tests without heavy gates.

- **Plugin candidate manual-test deployment**: Recorded the owner-requested canonical Pibo2 test deployment in the [execution ledger](/plans/unified-plugin-system-execution.md#owner-requested-pibo2-test-deployment-2026-09-13), including source commit, package checksum and authenticated API/headful readiness evidence. The Goal ownership finding and full remote acceptance remain open.

## 2026-09-12

- **Plugin candidate orchestration handover**: Preserved the implementation report, individual test-diff audit, baseline inventory and monitoring notes as linked artifacts in the [execution ledger](/plans/unified-plugin-system-execution.md#orchestrator-handover-audit-2026-09-12). Distinguished worker-reported local completion from pending independent review and final acceptance, recorded the budget-limited loop termination without restarting it, and added the independently observed Goal system-service ownership gap as an open implementation blocker.

- **Unified plugin-system local candidate**: Reconciled the [execution ledger](/plans/unified-plugin-system-execution.md) to the uncommitted integrated candidate. The Docker-only exact default suite completed normally with 2,963 tests, 2,954 passes, no failures or cancellations, and nine skips; package installation/import checks and the documented headful standard flows also passed. The final headful passes corrected and proved durable uninstall confirmation, retained placeholders, exact-hash reinstall, Terminal scroll/replay, ordinary replacement-shell activation, and restoration of the standard shell. AP00–AP18 have complete local implementation evidence but remain unaccepted behind AP19: real-provider Pibo2 execution and commit-backed current specification traceability are still required. No commit, push, PR, deployment, controller-gateway change, or production-data mutation occurred.

- **Unified plugin-system retirement pass**: Updated the [installation profiles](/project/installation-profiles.md) and [WSL guide](/project/guides/pibo-on-windows-via-wsl.md) to remove supported embedded-editor and Pibo VS Code extension setup. Marked the mixed [MCP/Pi-package](/specs/resources/external-mcp-and-pi-packages.md), [standalone terminal UI](/specs/operator/terminal-ui.md), embedded VS Code, extension, quickstart, and release documents deprecated against the [rebuild plan](/plans/unified-plugin-system-rebuild.md). Historical upgrade cleanup remains explicitly bounded; current package documentation no longer advertises those retired product surfaces.

- **Plugin migration test preservation**: Added D19, [PLG-TEST-001](/plans/unified-plugin-system-rebuild.md#plg-test-001-bestehende-tests-als-paritätsnachweis-erhalten) and AP19.7 to the rebuild plan and aligned the [execution ledger](/plans/unified-plugin-system-execution.md#existing-test-preservation). Existing tests remain the primary parity contract; additions supplement them, unavoidable adaptations need explicit evidence, and all twelve checkpoint regressions require individual resolution.

- **Plugin rebuild continuation authorization**: Corrected the [handoff](/reports/plugin-system-rebuild-checkpoint-2026-09-12.md) and [execution ledger](/plans/unified-plugin-system-execution.md): the owner explicitly authorized full implementation with subagents and removed the old session’s closure restrictions. Historical checkpoint evidence remains historical; normal environment and release rules continue to apply.

- **Plugin activation scopes**: Clarified the [rebuild plan](/plans/unified-plugin-system-rebuild.md#plg-act-001-systemweite-aktivierung-und-agent-auswahl) and [execution ledger](/plans/unified-plugin-system-execution.md#activation-scope-clarification-2026-09-12): system-only, agent-only and mixed plugins use independent system activation and agent selection. Goal keeps its app-wide function while tooling is selected per agent. Updated view availability, lifecycle/dependency requirements and work packages; added open A38–A42 acceptance scenarios. This is planned behavior, not an implementation claim.

- **Plugin rebuild handoff audit**: Audited the [checkpoint](/reports/plugin-system-rebuild-checkpoint-2026-09-12.md) against all 97 source hashes, preserved and directly linked historical research, contracts, worker reports, fixtures and complete regression logs, added exact environment/reproduction instructions, and corrected stale dispatch and integration claims in the [execution ledger](/plans/unified-plugin-system-execution.md). The rough effort estimate is 25–35%; all 37 product acceptance scenarios remain open. No product source, agents or deployments changed.

- **Plugin rebuild checkpoint**: Added the [development checkpoint report](/reports/plugin-system-rebuild-checkpoint-2026-09-12.md) and updated the [execution ledger](/plans/unified-plugin-system-execution.md) with the owner's revised concurrency and quota-window limits, focused SDK/package/API evidence, and outstanding service-lifecycle and generation-pinning integration requirements. Full product acceptance remains open.

- **Plugin rebuild execution**: Imported the owner-provided [complete rebuild plan](/plans/unified-plugin-system-rebuild.md) and its [architecture research](/reports/cordis-plugin-architecture-feasibility-2026-09-11.md), and started the [implementation and acceptance ledger](/plans/unified-plugin-system-execution.md). The ledger assigns five parallel implementation streams and retains all integrated acceptance gates as open.

## 2026-09-09

- **Cache diagnostics review**: Added the [PR 996 review and acceptance record](/reports/cache-observability-pr996-review-2026-09-09.md), documenting the reproduced same-millisecond receipt ordering defect, its correction, and validation.

- **Provider cache observability**: Corrected model-inference diagnostics so cache writes are never counted as cache hits, added cache-read ratio and conservative provider-counter warnings across replay, Compact Terminal, `pibo debug trace`, and the new bounded `pibo debug cache` command. Updated the [Terminal projection contract](/specs/web/trace-terminal-scrolling-and-workflow-projection.md#requirement-web-trace-debug-006), [Debug CLI contract](/specs/operator/debug-web-and-pty.md#requirement-op-debug-002), [Compact Terminal design](/project/design/compact-terminal.md#debug-metric-signal-rails), and [runtime-neutral debug reference](/project/agent-runtime-history-and-debug.md#debug-workflow). The Docker and headful browser fixtures use normalized provider usage and do not claim a live cache hit, identify the cause of a drop, enforce prefix stability, modify Pi/Codex/OMP, or deploy to production.

## 2026-09-08

- **Request-scoped HTTP failure containment**: Updated the [authenticated Web host contract](/specs/gateway/web-host-and-channel.md#requirement-wp02-gw-web-007-http-failures-remain-inside-their-request-or-upgrade-boundary). Responses now reject duplicate startup across destroyed, ended, finished, and headers-sent states; post-header stream failures, regular early termination, and disconnects during pending compressed-body buffering cancel and unlock the body reader before any late header write; Node handler and upgrade failures remain socket-scoped; and every fire-and-forget HTTP entry promise has a nonthrowing terminal rejection boundary. Bounded diagnostics omit body, header, cookie, credential, query, and error-message content. Validation used deterministic Docker tests, not production incident data or deployment.

- **Expired orphan yielded-run jobs**: Updated the [reliability contract](/specs/data/reliability.md#requirement-wp02-data-rel-006-run-jobs-and-run-records-remain-atomic-or-terminally-reconciled), [Debug CLI contract](/specs/operator/debug-web-and-pty.md#requirement-op-debug-006-run-job-diagnostics-expose-orphan-liveness-and-gate-cleanup), and [Gateway status contract](/specs/gateway/web-host-and-channel.md#requirement-wp02-gw-status-008-gateway-health-separates-degraded-run-job-reliability-from-active-work). New run creation is transactional across enqueue, claim, and run insertion; startup and explicit dry-run/apply maintenance move only expired missing-parent jobs to `orphan_run_job` dead letters; inspection reports claim expiry, missing records, effective liveness, and degraded counts without treating them as active work or replaying effects. Docker validation used deterministic synthetic temporary stores, not production incident records.

- **Durable message queue recovery**: Made terminal-output and lease recovery monotonic, isolated unresolved FIFO barriers from Room/global wait-age overload, added separate durable queue status/doctor health, and added an audited dry-run-first recovery CLI that never replays ambiguous work, fenced selected successors with live leases, preserved newer Session status, and made active barriers visible independently from paged terminal context. Updated the [Composer admission contract](/specs/web/composer-delivery-files-and-media.md#requirement-web-composer-admission-006), [capacity contract](/specs/runtime/capacity-and-scheduling.md#requirement-run-cap-005-fifo-barriers-are-explicit-and-isolated-from-wait-age-overload), [gateway status contract](/specs/gateway/web-host-and-channel.md#requirement-wp02-gw-status-007-runtime-and-durable-message-queues-have-separate-health), [operator CLI contract](/specs/operator/operator-cli.md#requirement-op-cli-006-interrupted-durable-messages-have-an-audited-recovery-workflow), and [operations runbook](/project/agent-runtime-operations.md#recover-an-interrupted-durable-message-queue). No production data access, gateway restart, deployment, or automatic side-effect replay was performed.

- **Output identity stability**: Updated the [Product Store contract](/specs/data/product-store-history-and-read-models.md) and [Reliability contract](/specs/data/reliability.md) with deterministic assistant-part and compact-result identities, literal legacy replay compatibility, redacted collision provenance, explicit audited reconciliation, and degraded collision health. Historical 3.5.0 assistant producer attribution remains unverified; reproductions are source-derived.
- **Bounded SQLite maintenance**: Added the [SQLite maintenance contract](/specs/data/storage-maintenance.md) for bounded status samples, killable verification, checkpoint pressure, atomic retention auditing, evidence preservation, and report-only payload-orphan candidates. No online payload files or metadata are deleted.

- **Run-reminder queue coalescing**: Updated the [runtime capacity contract](/specs/runtime/capacity-and-scheduling.md#requirement-run-cap-005) so stale internal run reminders are replaced with one current generation-scoped snapshot without weakening ordinary message limits. Internal reminder admission failure now retains pending run state and exposes one bounded runtime/operator warning instead of an unscoped terminal Session error; deterministic fake-clock and routed persistence/signal/trace regressions cover eventual delivery and non-consuming replacement. The [validation report](/reports/run-reminder-admission-validation-2026-09-08.md) records 105 backend tests and headful Chat Web/CDP acceptance of a still-running user turn during genuine reminder deferral.

- **Session trees and progressive image inspection**: Aligned layer controls before status lamps, added decorative tree connectors without changing indentation, restored five-line tool previews with expansion-triggered payload loading, and added grouped image thumbnails plus upload-name dialogs. Updated the [Session tree contract](/specs/web/rooms-and-session-trees.md), [Terminal contract](/specs/web/trace-terminal-scrolling-and-workflow-projection.md), and [file UX contract](/specs/web/composer-delivery-files-and-media.md). Isolated headful Chromium checks covered 1440×1000 and 390×844 viewports, real uploads, persisted image payloads, dialog navigation/focus restoration, and zero payload-reader requests before expansion versus one afterward. This is not physical-device Safari or host deployment acceptance.

- **Model-inference Debug diagnostics**: Added independently persisted Tool-call and model-inference diagnostic selections under the global Debug gate. Normalized provider usage now remains attached to its related trace owner and renders `IN`, `CACHED`, `UNCACHED`, and `OUT` in the Compact Terminal without adding pagination rows. Updated the [Terminal projection contract](/specs/web/trace-terminal-scrolling-and-workflow-projection.md#requirement-web-trace-debug-006) and [Compact Terminal design](/project/design/compact-terminal.md#debug-metric-signal-rails). The deterministic headful fixture validates local projection and rendering, not a live provider call or production deployment.

- **Final performance acceptance**: Added the [integrated acceptance report](/reports/performance-scalability-final-acceptance-2026-09-08.md), recording real Luna/low concurrency, deterministic mixed writes, complete large-message references and bounded reading/downloads. Updated the [Terminal contract](/specs/web/trace-terminal-scrolling-and-workflow-projection.md), [deployment seed contract](/specs/compute/deployment-pool.md) and [plan](/plans/pibo-performance-and-scalability.md). The user deferred the two-hour soak; sustained statistical capacity remains unproven. Reconciled package A's original acceptance history into the final integration and restored its original Pibo2 screenshot without changing the tested program code.

- **Performance review corrections**: Added the [renewed A–H review](/reports/performance-scalability-review-2026-09-08.md), recording reproduced lifecycle, navigation, paging and maintenance defects, forward-propagated fixes and renewed validation. The report qualifies the earlier capacity-proof conclusion: integrated load, soak and successful real-provider acceptance remain open.

## 2026-09-07

- **Chain review**: Added the [A–H chain review report](/reports/performance-scalability-chain-review-2026-09-07.md); records the critical review of all eight stacked performance pull requests against the plan budgets and existing contracts, the three confirmed defects corrected with deterministic regressions in packages B, F and G, the findings reported without a code change, the resulting re-acceptance requirement and the recommended merge order.

- **Runtime-isolation decision**: Added the [package-H measurement decision](/reports/performance-scalability-runtime-isolation-decision-2026-09-07.md); records that the assembled B–G event-loop, fairness, memory and recovery evidence stays inside PERF-LOOP-001 and the runtime ramp gate, so a documented capacity proof suffices and Pibo-managed runtime-host isolation is deferred with explicit revisit triggers.

- **Bounded maintenance**: Added the [package-G validation report](/reports/performance-scalability-maintenance-2026-09-07.md) and its artifacts; records the resumable bounded telemetry maintenance worker, SQLite online backups with external payload manifests, opt-in scoped capture storage, bounded manual prune and backup WAL quotas, and the isolated Pibo2 slot acceptance (lease released).

- **Isolated read models**: Added the [package-F validation report](/reports/performance-scalability-reads-2026-09-07.md) and its artifacts; records the isolated read worker, resumable history and unread projections, bounded SSE/HTTP streaming, the startup sequence-repair index fix, ten-million-event load evidence, and the isolated Pibo2 slot acceptance (lease released).

- **F–H handoff**: Added the [F–H continuation handoff](/reports/performance-scalability-fgh-handoff-2026-09-07.md) after the rework session hit the provider quota and a run-reminder loop (#967); records accepted packages A–E with open stacked PRs, the exact F and G work-in-progress, branch topology, and remaining gates.

- **Isolated telemetry**: Updated the [diagnostic worker contract](/specs/data/telemetry.md), [durable output checkpoint contract](/specs/data/reliability.md) and [package-E validation report](/reports/performance-scalability-telemetry-2026-09-07.md), preserving failed load evidence and the remaining integrated acceptance boundary.

- **Runtime capacity**: Added the [bounded admission, Room scheduling and provider-capacity contract](/specs/runtime/capacity-and-scheduling.md) and [package-D validation record](/reports/performance-scalability-capacity-2026-09-07.md), including control reservations, cold-start cancellation and the remaining integrated measurement boundary.

- **Durable message admission**: Added the [versioned receipt and recovery contract](/specs/web/composer-delivery-files-and-media.md#requirement-web-composer-admission-006) and [package-C validation record](/reports/performance-scalability-commands-2026-09-07.md), separating committed acceptance, runtime state and unknown client outcomes.

- **Storage integration acceptance**: Recorded [204 passing integration tests and renewed exact-candidate Pibo2 evidence](/reports/performance-scalability-storage-final-2026-09-07.md), including Composer, duplicate bursts, health and durable output after reload.

- **Performance continuation**: Preserved the [A–H handoff and local integration correction](/reports/performance-scalability-handoff-2026-09-07.md), including explicit persistence barriers, 131 passing Web tests and 39 passing storage/recovery regressions; renewed Pibo2 acceptance and C–H remain outstanding.
- **Snapshot-bound production restart approval**: Updated the [gateway restart contract](/specs/security/gateway-admission-and-restart.md#requirement-sec-gw-004-bind-production-restart-approval-to-freshly-inspected-work) for issue #914. Force now discloses and rechecks active event, queue, and run identities; rejects stale or incomplete approval; and records restart decisions before invoking the service manager. Dev restart remains ungated. No production restart or deployment.

## 2026-09-06

- **Compaction details and navigation**: Replaced the completed Compaction line with durable per-segment Tool-call statistics, runtime-reported Compaction tokens, and an expandable Markdown summary. Added a Compaction counter that cycles and focuses matching Terminal rows. Updated the [Terminal projection contract](/specs/web/trace-terminal-scrolling-and-workflow-projection.md#requirement-web-trace-compaction-010) and [Compact Terminal design](/project/design/compact-terminal.md#completed-compaction-summary).

- **Indexed admission implementation**: Recorded the [P0 implementation and isolated validation](/reports/performance-scalability-p0-2026-09-06.md), including synthetic million-event lookups, concurrency regressions and remaining acceptance gates.

- **Production admission and concurrency audit**: Added the [read-only production investigation](/reports/production-performance-audit-2026-09-06.md) and [performance/scalability plan](/plans/pibo-performance-and-scalability.md). Confirmed the synchronous room scan, cross-session blocking, and a separate cold-runtime admission delay; defined staged fixes, durable admission, resource budgets, load tests, and release gates. No product-code change, database/session deletion, gateway restart, or production deployment.

- **Selectable Tool payload calculation**: Added `Settings > Debug` controls for bounded character-count factors or opt-in Tiktoken encodings. Each Tool signal rail now records and displays its calculation basis, while legacy metrics remain readable and provider usage/billing stays distinct. Updated the [Terminal projection contract](/specs/web/trace-terminal-scrolling-and-workflow-projection.md#requirement-web-trace-debug-006) and [Compact Terminal design](/project/design/compact-terminal.md#debug-metric-signal-rail).
- **Configurable Tool Debug thresholds**: Added a dedicated `Settings > Debug` panel with the persisted Debug toggle, validated Time/Input/Output severity thresholds, default restoration, and explicit estimated-token caveats. Updated the [Terminal projection contract](/specs/web/trace-terminal-scrolling-and-workflow-projection.md#requirement-web-trace-debug-006) and [Compact Terminal design](/project/design/compact-terminal.md#debug-metric-signal-rail).
- **High-contrast Tool Debug signals**: Replaced quiet gray Tool metrics with a neon signal rail for rapid scanning: violet Time, cyan Input, lime Output, then yellow, orange, and hot-pink severity escalation. Updated the [Terminal projection contract](/specs/web/trace-terminal-scrolling-and-workflow-projection.md#requirement-web-trace-debug-006) and [Compact Terminal design](/project/design/compact-terminal.md#debug-metric-signal-rail); collection and token-estimation behavior are unchanged.

- **Room edit ownership**: Added [field-scoped Room edit and navigation guarantees](/specs/web/app-shell-bootstrap-navigation-and-pwa.md#requirement-web-shell-room-update-008-room-edits-preserve-navigation-and-unrelated-state) and the [exact-candidate validation report](/reports/room-edit-ownership-validation-2026-09-06.md) for issue #938. Records the local 29.9-second stale-name reproduction, 30 public desktop/mobile cases, 15–22 ms bounded feedback, real Spark Queue/streaming/workspace checks, and remaining tail Long Tasks. No merge, release, or controller deployment.
- **Room creation ownership**: Preserved optimistic Rooms, scoped failure rollback, and released creation controls before navigation hydration. Added the [Room creation contract](/specs/web/app-shell-bootstrap-navigation-and-pwa.md#requirement-web-shell-room-create-007-room-creation-preserves-later-navigation) and [local/Pibo2 evidence](/reports/room-creation-ownership-validation-2026-09-06.md), including trusted desktop/mobile input and real Spark Queue/streaming checks.
- **Post-create navigation ownership**: Made Session hydration nonblocking after POST and invalidated superseded bootstrap selection immediately. Added the [shell creation contract](/specs/web/app-shell-bootstrap-navigation-and-pwa.md#requirement-web-shell-create-006-post-create-hydration-does-not-own-later-navigation) and [exact-candidate validation](/reports/session-creation-ownership-validation-2026-09-06.md): local regression, 22 public-path desktop/mobile cases, real Spark tool/Queue, and separate unresolved gateway-status scaling evidence.
- **Terminal viewport and wheel ownership**: Added the [implemented scroll-owner contract](/specs/web/trace-terminal-scrolling-and-workflow-projection.md#requirement-web-trace-viewport-009) and [exact-candidate validation](/reports/terminal-viewport-and-wheel-validation-2026-09-06.md) for external viewport shrink, coarse wheel pending anchors, historical prepends/reload, and genuine Spark streaming/Queue. Reports preserve the 11 ms observed queue drain and the limits of the broader performance investigation.
- **Signal projection cost**: Reused already-listed Session records for ancestor depth instead of preparing per-record SQLite queries. Added the [status projection contract](/specs/gateway/web-host-and-channel.md#requirement-wp02-gw-status-006-signal-projection-reuses-its-complete-listed-view) and [local/Pibo2 validation](/reports/gateway-status-scaling-validation-2026-09-06.md), including preserved active-work safety, real streaming, and Queue under status load.

## 2026-09-05

- **Terminal initial visibility**: Removed redundant initial-index settlement while retaining sticky-scroll positioning; published [exact-candidate headful Pibo2 evidence](/reports/terminal-initial-visibility-validation-2026-09-05.md) for large-history switching, reload anchors, real Spark streaming and Queue. The separate pre-existing mobile viewport-shrink gap is tracked in issue 928, not claimed fixed.
- **Session status latency**: Published [Docker and headful Pibo2 validation](/reports/session-status-latency-validation-2026-09-05.md) for credential-scoped, bounded provider quota caching; warm status median fell from 490.8 to 28.3 ms in sampled public requests, while the broader large-session and intermittent queue-lag investigation remains open.
- **Idle Session history latency**: Added [adapter-owned cold fork inspection](/specs/runtime/adapter-contract.md#requirement-run-spi-005), [bounded Pi history caching](/specs/runtime/pi-adapter.md#requirement-run-pi-005), and [passive Terminal usage reads](/specs/web/trace-terminal-scrolling-and-workflow-projection.md#requirement-web-trace-passive-007). The [validation report](/reports/idle-session-history-latency-validation-2026-09-05.md) records 228 Docker tests, exact candidate parity, headful Pibo2 cold/warm timings, and real Spark queue/reload evidence while keeping broader streaming and optimistic-navigation gaps explicit.
- **Tool-call Debug mode**: Added a persisted Debug toggle beside Thinking, replaced duplicate topbar Raw Events and view controls with workspace-tab navigation, and documented per-invocation duration and explicitly estimated payload-token counts in the [Terminal projection contract](/specs/web/trace-terminal-scrolling-and-workflow-projection.md#requirement-web-trace-debug-006). Metrics survive live/replay paths without browser tokenization; legacy and unmeasurable values remain unavailable.

- **Session Preview auto-open**: Extended [Session Live Previews](/specs/compute/session-live-previews.md#requirement-cmp-preview-006) with an authenticated Session-scoped creation stream so a newly created Preview opens the deduplicated Desktop Preview tab only for the still-selected Pibo Session; background Sessions, mobile layouts, and pre-existing Previews remain non-opening.
- **Compute worker Preview authentication**: Added `pibo preview expose-worker` and an internal proxy mode that preserves Preview credential isolation while allowing a validated local-auth Pibo compute worker to accept HTTP and WebSocket traffic without a second Google login; updated the [Session Live Previews and Safe Proxy specification](/specs/compute/session-live-previews.md).
- **Running-session forks**: Added capability-gated snapshot forks for completed user messages while Pi or Codex Native continues an active turn; the active message remains excluded, the source binding stays attached, and OMP retains idle-only fork semantics. Updated the [routing contract](/specs/gateway/routing-events-and-actions.md), [Pi adapter](/specs/runtime/pi-adapter.md), [Codex Native adapter](/specs/runtime/codex-native-adapter.md), and [OMP adapter](/specs/runtime/omp-adapter.md).
- **Preview production setup**: Added a supported production activation flow with exact wildcard-DNS guidance, bounded Caddy on-demand TLS authorization, public DNS/TLS/routing diagnostics, and the stable [Session Live Preview operations runbook](/project/session-live-previews.md).
- **Transition closure**: Published the [Session-native validation report](/reports/session-native-workflow-transition-validation-2026-09-05.md) with final-code root results (2,747 total; 2,742 passed; 0 failed; 5 skipped), standalone package installation, and headed real-execution evidence; archived the original directive body without changing the main checkout. No production deployment.

- **Session-native product model**: Replaced the current Projects product contract with [Rooms and normal Session trees](/specs/web/rooms-and-session-trees.md), including Room workspace defaults and canonical Session continuity.
- **Workflow ownership**: Recast [Workflow catalog and execution](/specs/orchestration/workflow-catalog-and-session-execution.md) around normal Pibo Sessions and one Workflow store, and aligned the Web UI, runtime, product-store, shell, projection, validation, and follow-up documents.
- **Navigation cleanup**: Removed the empty Projects parity change directory and regenerated its owning indexes.
- **Integrated traceability**: Rebound affected specifications to checked code commit `14cbaf0fd04cfa321674b570baeb40e543d957cb`, exact current symbols, and renamed tests; documented pending configured starts, canonical manual-run facts, inspection-derived headers, and recoverable two-target migration durability.
- **Validation evidence**: Recorded the passing clean build, all typechecks, 144 Workflow package tests, 56 focused migration/storage/router/header tests, 62 focused UI source tests, complete isolated root suite at `14cbaf0f` (2,744 total; 2,739 passed; 0 failed; 5 skipped), and completed headed Room, Workflow Session, desktop/mobile, and normal provider-backed Session checks.
- **Final manual Workflow acceptance**: Rebound changed contracts to `7ec71c2cca2108423002be0e7330d2a20c4c5b67` after upstream #911/#912 integration; documented the defaulted Run Room selector, optional API Room/workspace, write-permission and workspace-inheritance behavior, persistent pending explanation, actual `openai-codex` manual execution, canonical completed inspection, viewport fit, and clean `npm install --omit=dev` smoke. The final-code whole-root rerun remains underway and unclaimed.
- **Shared observation ownership**: Clarified that live agent observation and persisted debug inspection are adapters over one normalized observation/query core, so source-independent improvements apply to both unless a documented lifetime or durability constraint prevents parity.
- **Unread delegated-agent observation**: Made `cursorMode="auto"` the live Observe default with durable per-parent, per-query cursors, explicit non-mutating `history` replay, bounded cursor-scope persistence, hidden tools by default, and stateless operator debug observation; updated the delegated-agent, Session persistence, and product-store schema contracts.

## 2026-09-04

- **Observe regex filtering**: Added and review-hardened the optional `textRegex` contract for [delegated-agent observation](/specs/orchestration/subagents.md#requirement-orch-sub-005), preserving case-insensitive `textContains` and conjunctive behavior while representing empty normalized text as one record, bounding dense matching and streaming pagination, rejecting unsafe NUL boundaries, and resolving optional rg binaries only for regex use.
- **Runtime dependency refresh**: Updated the [Codex Native](/specs/runtime/codex-native-adapter.md), [OMP](/specs/runtime/omp-adapter.md), and [Pi](/specs/runtime/pi-adapter.md) contracts for validated Codex 0.153.2, OMP 18.1.10, and Pi 0.85.0 compatibility, including exact version diagnostics and the Pi server package required by the published 0.85.0 entrypoint.
- **Operator and architecture guidance**: Refreshed the [runtime operations guide](/project/agent-runtime-operations.md) and [adapter architecture record](/project/architecture/agent-runtime-adapters.md) to remove stale Codex 0.147.0 operational wording and record the current stable native-tool inspection boundary.

## 2026-09-03

- **Payload identity correction**: Updated the [product store and payload contract](/specs/data/product-store-history-and-read-models.md#requirement-wp02-data-store-003) to isolate equal bytes with different content types or retention classes, preserve indexed semantic deduplication, and migrate legacy SHA-only uniqueness without rewriting payload files.
- **Security contract correction**: Narrowed the [sensitive-output redaction policy](/specs/security/private-files-and-http.md#requirement-sec-file-004-narrow-credential-redaction-protects-identified-output-sinks-without-treating-product-identifiers-as-secrets) to strong credential signals, preserved ordinary `pibo-*` identifiers and paths, and recorded the pre-persistence threat-model decision and sink-coverage limit.

## 2026-09-02

- **Project documentation rules**: Replaced migration-era instructions in `AGENTS.md` with concise OKF v0.2 guidance that delegates detailed workflow rules to `maintain-okf-docs` and the documentation profile.
- **Integration correction**: Converted the three sidebar browser-tab validation reports to conformant OKF concepts and moved the completed inventory, specification/design, and task ledger into [legacy change history](/legacy/specs/changes/sidebar-browser-tabs/).

## 2026-09-01

- **Upstream refresh**: Merged `upstream/dev` at `39090b8850758293e69380a52bb7498d7c955bc2` into the accepted migration chain, classified all six upstream documentation modifications, preserved the historical agent-management body, and folded retired capability-document deltas into canonical current specifications.
- **Current-contract reconciliation**: Refreshed canonical specifications and traceability for durable output sequencing, retries, integrity repair, compute and browser lifecycle, signals and routing, runtime workers, resources, Project lifecycle, and Chat/CLI accessibility against current source and tests.
- **Package closure**: Updated strict-mode validation as the default documentation gate and retained package-owned compute-image inputs plus actual-archive link-closure coverage.
- **Migration closure**: Reconstructed the complete 762-record migration ledger with zero pending records, retired 13 superseded README controls, and generated the final 106-index navigation closure.
- **Evidence publication**: Registered 61 accepted C-REPORTS Evidence Reports in the immutable evidence manifest with hashes over their complete published files.
- **Independent audit closure**: Re-resolved all 44 upstream-impacted normative specifications at `39090b8850758293e69380a52bb7498d7c955bc2`, corrected stale pre-refresh execution accounting and one renamed schema-migration test locator, and retained explicit real-path, browser, external-provider, Windows, publication, and Pibo2 evidence boundaries.

## 2026-08-31

- **Validator/profile correction**: Corrected the [preserved-body link rule](/project/documentation-profile.md#preserved-body-link-exception) so an exact raw relative target with literal dot segments is suppressible only when inspection observes the same in-bundle missing link, and recorded the narrow future-correction authority in the [migration plan](/plans/okf-migration.md#standing-authorization-for-trivial-corrections).

## 2026-08-30

- **Review 10 remediation**: Made exact frontmatter envelopes portable across LF, CRLF, lone CR, and mixed Markdown line endings while preserving original body bytes.
- **Review 9 remediation**: Enumerated true ledger introductions across complete reachable Git history and bound ledger and evidence-manifest reads to stable, non-following file descriptors.
- **Review 8 remediation**: Derived the pending-byte trust anchor from ledger-introduction history, required complete conformant index chains, rejected symlinked JSON control paths before reads, made core logs CommonMark-fence-aware, and closed every local link in the installed documentation subset.
- **Review 7 remediation**: Bound all pending records to SHA-256 hashes derived from the declared Git base, required migration-time conformant index and stable-evidence registration, and rejected every unsafe repository Markdown path before reads.
- **Core conformance**: Corrected present `log.md` validation to require the normative date-grouped list-entry structure described by OKF v0.2.
- **Packaging**: Removed the stale pending VS Code release runbook from installed package contents while retaining all three README-linked installation guides.

## 2026-08-29

- **Creation**: Established the OKF v0.2 bundle root and [Pibo documentation profile](/project/documentation-profile.md).
- **Creation**: Added the [OKF migration plan](/plans/okf-migration.md), [foundation status](/project/status/okf-migration-foundation.md), machine-readable ledger, validator, and templates.
- **Correction**: Preserved the five project-approved roots and replaced the provisional top-level function directories with nested project and reports paths.
- **Correction**: Replaced the invalid provisional OKF source URL with the verified pinned `knowledge-catalog` source and recorded upstream and controller-local hashes without claiming byte identity.
- **Validation**: Added ledger-independent OKF core validation, deterministic ledger-owned index generation/checking, explicit log checking, stronger migration-ledger invariants, and the narrow immutable preserved-body link exception.
- **Authoring**: Exposed the full approved type vocabulary, globally prefixed requirement IDs, and thin `.codex` wrappers around the canonical specification-writing skill.
- **Authoring**: Replaced the obsolete capability catch-all path with canonical `specs/<domain>/<spec-name>.md` ownership, bounded confidence to `high|medium|low`, and required concrete source, test, build, browser, or Pibo2 evidence instead of an undefined verification scale.
- **Independent review remediation**: Closed F-001 through F-006 by narrowing preserved-link suppression, binding specification evidence to real Git commits and files, rejecting index metadata injection, aligning templates, enforcing a Pibo dated-log minimum, and assigning path-specific host-exception reasons.
- **Integration correction**: Accepted requirement IDs with two or more uppercase semantic components before the numeric suffix, without requiring a literal `REQ` component.
- **Review 2 remediation**: Added reverse body-heading validation for traced requirements.
- **Review 3 remediation**: Replaced heading heuristics with the explicit `Requirement: <ID>` grammar, enforced one body heading per traced ID, ignored fences and HTML comments, and made index generation preflight all managed outputs before any write.
- **Review 4 remediation**: Prohibited raw HTML comment delimiters in current specifications, made index preflight ledger- and real-path-aware, rejected symlinked/non-regular targets, and rendered concept metadata as structure-safe plain text.
- **Review 5 remediation**: Applied CommonMark fence recognition, made index preflight recursive and globally ledger-complete, rejected invisible or direction-spoofing metadata, and required an exact commit-preserving worker Git mirror.
- **Review 6 remediation**: Treated LF, CRLF, and lone CR uniformly during specification scanning and rejected U+2800 plus the explicit visually blank filler set in index metadata.
- **Relocation**: Moved two guides and four operator runbooks from top-level `guides/` and `ops/` into `project/guides/` and `project/operations/`; their concept conversion remains pending.

- 2026-09-13: Parent review of optional plugin modules found and corrected legacy host-skill migration classification; see `/plans/unified-plugin-system-execution.md`. Existing references remain preserved; unavailable references produce warnings instead of blocking unrelated plugin selection.

- 2026-09-13: Corrected candidate `112ca544` accepted on Pibo2 for optional modules, mobile configuration and existing-agent migration; evidence in `/plans/unified-plugin-system-execution.md`.

- 2026-09-19: Dev-Gateway-Deploy mit 30-Minuten-Muse-Turn-Timeout (requestTimeoutMs-Default 1800000, rev24 von pibo.runtime-muse-native aktiv, Hash sha256:4f5dbc46...). Beim ersten Restart wurde das Update erneut aufgeschoben (deferred), weil 6 idle/error-Sessions stale reserved Generation-Admissions aus abgestorbenen Turns hielten; nach Backup (/tmp/pibo-dev-backup-20260919-125351) wurden sie per releaseSupersededSessionAdmissions freigegeben (gleiche Semantik wie Session-Rebind), danach griff das Update beim zweiten Restart ohne Blocker. pibo.runtime-pi ebenfalls auf rev12 aktualisiert.

- 2026-09-19: Muse-native-Session-Rescue: 6 alte Sessions (u. a. ps_cde8d951) scheiterten mit "native session absent but cannot import durable Pibo history". Ursache: native Sessions lagen unter generationstemp XDG_DATA_HOME und wurden beim Dispose unsichtbar (Spec-Known-Limit bestätigt). Fix: XDG_DATA_HOME ist jetzt session-stabil (<sessionRoot>/xdg-data, pro Pibo-Session isoliert); überlebende Stores aller 6 Sessions migriert (Backup /tmp/muse-native-instance-backup-20260919); mechanisch per session/read verifiziert (01a0b924, 01a0b909 resolven). Rev27 aktiv, kein Deferred. Verbleibend: echtes historyImport für restlos verlorene natives (Spec-Zeile bleibt bestehen).

- 2026-09-19: Diagnose ps_00203e13 ("Iteration Plan", nur lesend, nichts verändert): manuelle Compaction um 10:07:16Z lief nativ ERFOLGREICH (compaction_end aborted:false mit Stats); der Crash kam 26s später beim Folgeturn ("turn/start runtime submit failed: event id 834d77f4... conflicts with an existing event", nativ-seitig, transient). Native Session 01a0b8d8 gesund (HEAD healthy, Journal vollständig ab Ordinal 1, Snapshots identisch); Binding bound, Session im Router idle. Kein Rollback nötig/möglich (Journal append-only mit Integritäts-Hashes, Pibo-Log append-only mit Fingerprints); Empfehlung: einfach per neuer Nachricht retryen.

- 2026-09-19: Diagnose ps_6d98110f ("Sandbox Check", nur lesend): Turn starb nach exakt 60s mit "Runtime capacity wait deadline exceeded" im PROVIDER-Pool (erwiesen: kein message_started, Acquire steht vor dem Emit in routed-session.ts:1091/1096). Raum room_4bfe99b2 hielt 4 Provider-Leases (cde8d951, f8f6c3d1, 60529962, c67ca345); General-pro-Raum-Cap ist perRoom-min(reserved,1,perRoom-1)=4, daher 5. Turn im selben Raum nie eligible. Keine Leaks, keine Children; lange Turns (30-Min-Timeout) halten Leases länger und sättigen Räume eher. Retry sobald ein Raum-Turn endet.

- 2026-09-19: Agent-Designer-Revert-Escape (`fix/agent-designer-blocker-revert` auf Beta-Basis `1d157641`): `Revert`-Button im Designer-Header stellt bei nicht speicherbarem Draft (z. B. Skills + Muse-Runtime) den letzten Serverstand wieder her bzw. verwirft ungespeicherte Neuanlagen; Navigations- und Tab-Guards verweisen in der Fehlermeldung auf Revert. Kein stilles Auto-Verwerfen. Nachweis: `test/chat-ui-agent-designer-revert.test.mjs` 3/3 (ohne Fix rot verifiziert), Designer-Suites 33/33, `chat-ui:typecheck` ohne neue Fehler, Gateway-Smoke auf 4828/4829 mit 200ern. Siehe `/reports/agent-designer-revert-escape-validation-2026-09-19.md`.

- 2026-09-19: OKF-Validator-Performance (Branch perf/okf-validator-batched-git, Worktree .worktrees/okf-validator-perf): Traceability-Evidenz wird jetzt mit einem git ls-tree -r pro Commit statt einem Prozess pro Pfad geprüft, dazu pro-Lauf-Caches für Commits, Bäume, Pfad-Existenz, Dateiinhalte und Verzeichnisauflösung; strict 206 s auf ~17 s, migration 203 s auf ~16 s, indexes-check ~26 s auf ~6 s. Alle drei Modi liefern byte-identische Diagnose-JSONs wie vorher, 87/87 Validator-Tests grün, Details im Validation Report docs/reports/okf-validator-performance-optimization-2026-09-19.md. Drive-by: veralteter Managed-Index docs/specs/runtime/index.md regeneriert; ein vorbestehender PIBO_TRACE_COMMIT-Fehler in docs/specs/data/storage-maintenance.md bleibt unverändert bestehen.

- 2026-09-21: Remote-Gateway-Redeploy nach Reboot: Beta-4.0-Code (`47342db6`) mit Home `/root/.pibo-remote-oauth-test` gestartet; zwei Kompositions-Abstürze behoben (stale `@pasko70/pibo`-Symlink, 4 verwaiste `reserved`-Admissions blockierten Plugin-Updates mit folgendem `Unknown tool "pibo_gateway_send"`); `ECONNREFUSED 4789` via fehlende `PIBO_REMOTE_AGENT_GATEWAY_PORT=5789` behoben; E2E `CONNECTIVITY_OK` über REST verifiziert; Queue-Ablehnung `oldest_wait_age` als normale Session-Admission (10-Min-Limit, keine Remote-Queue) eingeordnet. Voller Bericht in `/reports/incident-2026-09-21-remote-gateway-redeploy.md`.

- 2026-09-21: Nachtrag zum Remote-Gateway-Incident: erneute Send-Timeouts (9× 120 s, 7 Sessions) plus 2× `oldest_wait_age`-Ablehnung als bekannter Mechanismus bestätigt (angenommen-aber-langsam, kein Gateway-Ausfall); `launch-control.json`-Ablehnung durch „Tool-Sicherheitsfilter" ist nicht Pibo-seitig (kein `ok=0`-Eintrag, gleiche Datei am selben Tag erfolgreich editiert, Raum läuft YOLO) und liegt in der Toolchain des externen Agenten. Integriert in `/reports/incident-2026-09-21-remote-gateway-redeploy.md`.

- 2026-09-21: Zweiter Nachtrag zum Remote-Gateway-Incident: 4 weitere Send-Timeouts (FINAL PASS A1–D1, 18:09–18:17Z) als bekannter Accepted-but-slow-Mechanismus bestätigt; Debug-CLI zeigte auf isolierten Runtime-Store, weil `pibo debug session` Stores nur via `PIBO_HOME`/`~/.pibo` auflöst (kein Override-Flag) und Runtime-Prozesse ein isoliertes `HOME` erhalten — Workaround `PIBO_HOME` pinnen. Integriert in `/reports/incident-2026-09-21-remote-gateway-redeploy.md`.
