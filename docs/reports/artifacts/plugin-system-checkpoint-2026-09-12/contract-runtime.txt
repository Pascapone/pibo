# Runtime handoff — AP06 implementation, 2026-09-12 06:33 UTC

## Implemented early dependency
`AgentRuntimeAdapterRegistry.unregisterInstance(instanceId): boolean` and `unregisterDriver(adapterId): boolean` exist. Synchronous registration-only removal; no session abort/resource disposal. Driver removal throws while definitions reference it. Core can wire ownership rollback.

## Actual default-path APIs now on disk
- `InitialSessionContext` / Options add `pluginSelection?: AgentPluginSelection`, `pluginSelectionRevision:number`, `pluginAgentId?:string`, `effectivePluginPlan?:EffectivePluginPlan`. Builder `.withPluginSelection(selection,revision,agentId)`. `createCustomAgentProfileDefinition` propagates Designer's **pluginSelection** and revision/id. No new selection truth.
- `src/agent-runtime/plugin-plan.ts`: `PluginRuntimeCoordinator({store:PluginStore,manager:Pick<PluginManager,'reserveGeneration'|'releaseGenerationAdmission'>,host:PluginHost})`; `.preview(profile,runtimeTarget,piboSessionId?)` is pure resolution; `.reserve(profile,runtimeTarget,piboSessionId,generation)` is synchronous atomic admission, revision crosscheck, selected host projection and immutable generation persistence; `.release(generation)` only after actual disposal.
- Router option **`pluginRuntime?: PluginRuntimeCoordinator`**. `createRoutedSession` now reserves before the first async setup and delivers projected profile to existing resource/tool service and Pi/Codex/OMP adapters. Migrated profiles reject missing coordinator rather than bypassing admission. Explicit-registry router no longer silently creates a fallback builtin runtime registry.
- Core host registration lookup is `host.contributions.get('contribution',qualifiedId)` -> `{installation,contribution,value}`. Values remain existing ToolProfileRegistration/SkillProfile/ContextFileProfile/SubagentProfile. `mcp-server` value is server name string or `{name}` using existing external-MCP config service. `hook` value is `RuntimePluginHook` below. Requirements checked before consuming values or factories.

## Orchestrator required composition hookup (owned files)
Construct ONE coordinator using the same persisted plugins Store, Manager and Host as product management. Pass `pluginRuntime` to PiboSessionRouter options in gateway/server composition. Do NOT construct a separate host/manager/catalog just for sessions. All migrated/default profile builders must provide explicit accepted selection; absent selection is legacy, not new manifest defaults.

## Build Context GET replacement — no inspection resource session
`src/agent-runtime/plugin-context-build.ts` exports:
- `inspectPluginBuildContext({store,piboSessionId,snapshotId?,preview?:{coordinator,profile,runtime}})` -> `{actual:PluginContextBuildSnapshot|null,preview:PluginContextBuildSnapshot|null,diagnostic?}`. `runtime` is a declared `PluginRuntimeTarget`, obtained from registered descriptor/static catalog, NOT `inspectAgentRuntimeInstances()`.
- `buildPluginContextPreview(plan)`, `capturePluginContextBuild({plan,resources,tools})`, `persistPluginContextBuild(store,snapshot)`, `exportPluginContextBuild(snapshot)` -> `{modelText,metadata,completeWirePrompt:false}`.
- Snapshot `{schemaVersion:1,snapshotId,kind:'actual'|'preview',createdAt,piboSessionId?,generation?,plan,nodes:PluginBuildProvenanceNode[],boundaries:string[]}`. Actual `build-*` snapshots are saved by router after adapter open. Hook evidence has `hook-*` IDs, separately persisted under same generation.

**Exact web-app replacement location:** `buildContextBuildSnapshotForRequest` currently creates `new PiboRuntimeResourceService()` + `createSession({sessionGeneration:'inspection-*'})` and calls `resolveContextBuildRuntime` which invokes runtime inspections. Remove BOTH from GET. After `requireSharedSession`, use helper above. Actual reads require only store and session ID; explicit preview may read current profile + registered static descriptor and call coordinator. Never create resources/adapter/runtime/MCP to refresh inspector. Response consumers need explicit actual vs preview fields (browser worker). Existing `inspectPiboContextBuild` now returns conservative pure legacy projection, not a fake Pi runtime snapshot. `inspectPiboProfile`, portable context snapshot and profile capability validation no longer invoke dynamic factories.

## Hooks actual controlled paths
`src/agent-runtime/plugin-hooks.ts`: `RuntimePluginHook={descriptor:PluginHookDescriptor,run(value,context):PluginHookResult|Promise<...>}`; context session/generation/tool/call identity + AbortSignal. Host contribution kind `hook`. Required comes from effective contribution, not executable value. `runPluginHooks` orders by order then qualified ID; validates every transform and provenance; bounded timeout/cancel; pre/input required failure closed; post failures retain observed execution. Tool hooks wrap only controlled Pibo tools (including yielded targets), not native harness tools. Router input transforms only text, never trusts plugin replacement of Session ID/provenance. Hook evidence is persisted without raw tool inputs/results.

## Pi discovery / compatibility
`pluginOnlyPiServicesOptions` + `pluginOnlyPiSettings` form the shared bootstrap/model-catalog policy. Implicit extension paths disabled, package/extension settings projected empty without mutating original user files. Explicit host factories preserved; user-skill/context paths preserved. Pibo-specific Pi-package runtime loader removed from Pi runtime. Native subagents remain separate; manual children still resolve target independently. External OMP MCP remains unsupported; Codex's internal session tool MCP is independent.

## Final validation checkpoint — 06:49 UTC
First raw npx tsc exhausted default Node ~1GB heap (runtime-tsc-1.log). Final project-standard `node --max-old-space-size=1200 .../tsc` PASSED; 54/54 focused tests/regressions PASSED inside worker under flock (`runtime-final-validation.log`). Full honest handoff: `worker-runtime.md`. Gateway coordinator injection and web-app actual/preview dispatch remain Orchestrator integration points; AP06 is not fully product-accepted. No Git/gateway/metadata/shared doc edits. Runtime source work is stopped for the requested orderly quota-reset handoff.
