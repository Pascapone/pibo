# Orchestrator integration decisions and review gates

## Live coordination — 2026-09-12 06:36 UTC

- Direct /steer messages delivered and acknowledged by all FOUR still-active child sessions at06:34. Browser yielded request had aborted but its actual child continued; no restarted/new sessions. Finish currentblock/tests/report andend.
- Parent now fixed browser root-entry/sibling backend exposure and dedicated browser subtree/backend overlap; three negative asset tests passed. Browser preview URL now plugin-plan?kind=preview. Please preserve those edits.
- Parent fixed core service-owner pinning gap: shared pure service-providers.ts now used by Host+Resolver; SDK input serviceProviders distinct from contribution providers, registry host.services.owners() projects actual owners. Runtime coordinator needs serviceProviders: host.services.owners() alongside services.versions() (parent will wire once owner done if not done). Do not start new work beyond current handoff. Focused revalidation pending. Old gap bullet below is historical context.
- Backend compile and SDK declarations passed06:32; parent11tests passed. Two web-gateway baseline assertions failed only because Docker PIBO_COMPUTE_WORKER=1 permits worker non-loopback dev auth; rerun that host-assumption unit fixture with env -u PIBO_COMPUTE_WORKER. Chat-UI noEmit typecheck passed06:31. No default product or visual acceptance.


UPDATE 06:21 UTC — OWNER STEERING: No new agents or follow-up workstreams. Let the five current agents finish cleanly; about 40 minutes remain before the owner's quota reset. The deliverable for this window is a coherent, tested checkpoint unless the full goal happens to finish. Do not hurry or abort existing work. Each current owner should complete its implemented block, run focused meaningful checks, explicitly record default-product integration gaps, and hand off. Do not broaden into later AP packages merely to keep working. Parent will consolidate and commit the stable milestone; full migration/AP11+/Pibo2 remain open where not proved. This update supersedes earlier instructions to keep assigning later extraction or extra reviews immediately.

- User reduced concurrency: at most FIVE delegated agents total, not five per type. Currently core, management, runtime, browser, designer are the five implementation owners. No further researcher starts; reuse completed workers for targeted follow-up and review.
- Parent now owns new `src/plugins/backend-loader.ts`, `product-services.ts`, `src/channels/types.ts` service lookup, and loader/integrated dispatch tests in addition to prior central files.
- Backend contract: prebuilt module exports named `setup(context)`. Parent's `startInstalledPlugins(host,installations,{providers?,definitions?})` freezes installation snapshots and uses Management's existing `createStagedPluginDefinition`; the duplicated draft loader has been removed. Validates full graph and ALL staged hashes before first import, and Management rechecks each artifact immediately before import. Same PluginHost and registry projection, no second activation map.
- `preparePluginSdkResolution(artifactRoot,packageRoot?)` creates the conventional node_modules/@pasko70/pibo link OUTSIDE hashed artifacts, to the actual installed Pibo package. No npm install scripts or user-source writes. Existing linkage to another package is rejected until a stopped composition boundary. Public SDK resolves via package exports. Installed browser shared React contract remains Browser owner.
- Gateway channel context now provides generic `getService<T>(id)` from the one host. Chat Web dispatches actual management routes before catalog/skill maintenance, preserving read-only inspection; requires existing login, same-origin for mutations, and exact stored session existence for session operations. Manager is resolved from service `pibo.plugins.management`, or explicit ChatWebAppOptions.pluginManager test/composition injection. No permissive local manager fallback. Root default composition service publication STILL needs integration.
- Core host currently supports complete start/stop only. Parent needs a safe controlled per-plugin activation/deactivation or affected-composition lifecycle for PluginManager, with graph validation, scoped cleanup, no teardown of unrelated live web/runtime services, no new generations across retirement. This is a required next Core integration/review task; do not pretend a stop-all host adapter is safe merely because the targeted plugin drained. Existing sessions and provider dependencies must remain accounted for. Root management routes stay unavailable when no host service is published.
- Parent added `tsconfig.plugin-sdk.json` plus `plugins:sdk:build`, included in full build, emitting public SDK/host declarations. First parent compiler attempt hit default Node 1GiB heap; use the repository's explicit `node --max-old-space-size=1200 node_modules/typescript/bin/tsc` in the 2GiB Docker worker. Second compile reached a concurrently edited Management helper error `plugin-management-routes.ts(47,34) Type T does not satisfy constraint object`; owning worker resolves, parent focused loader/dispatch tests separately underway against emitted code.
- Browser route review: a root-level browser entry (`browser.mjs`) currently sets browserRoot=artifactRoot and can serve `backend.mjs` or other private .json/.js files. Must fail closed: root-level entry may serve only itself, or mandate a dedicated browser-only subtree and prohibit backend inside it. Add negative tests for root entry/sibling backend, same subtree backend and encoded traversal. Do not accept A05/publiccatalog isolation on traversal tests alone.
- Session-plan read needs explicit kind. Default tab availability may use stored generation, but Build Context must request a separate pure preview even when a generation exists, so changed settings appear without mutating saved evidence. Proposed GET `/sessions/:id/plugin-plan?kind=preview`; default uses stored generation (pure preview only when none). Parent will route through pure runtime helper/service once exported, never createSession/getSessionRuntimeProfile merely to inspect.
- Core integration review found service revision gap: Resolver `visitPackage` validates service *versions* but never adds the chosen service provider to `infrastructure`/plan.plugins unless separately selected. Therefore generation admissions can omit a required service's actual owner. Required fix: record/infer explicit service owner choices from the SAME host graph (`serviceProviders` distinct from contribution `providers`), traverse required service dependencies, pin their artifact revisions, and include them in provenance/admission without enabling their agent tools. Ambiguous provider claims cannot be resolved from import order/version alone. Add a fixture where A requires a service from unselected B (no package dependency), and explicit replacement C proves correct owner/revision pinning and drain protection. Parent service consumer collector will additionally enumerate real host service registrations, but that does not replace generation pinning.
- Browser dispatcher now wired into real web-app before legacy profile/indexing maintenance. `?kind=actual` reads immutable generation only (404 if none), `?kind=preview` ALWAYS uses pure reader, omitted kind uses recorded generation or pure preview when none. `PluginSessionPlanReader` and service ID `pibo.plugins.session-plan` in parent-owned product-services.ts. Runtime should provide `(sessionId,kind)=>Promise<{plan,agentId?,roomId?}>`; only preview currently delegated to it. Explicit options.pluginSessionPlan is available for composition/tests. Manager service lookup is mandatory. Session access uses direct stored session existence, NOT legacy canonicalizeSessionProfile/getProfileInfos, which could invoke factories.
- Parent SDK declaration build + 8 loader/real Chat Web dispatch tests PASS at 06:09. Covers invalid graph no-import, all-artifact integrity before first import, SDK import from staged artifact outside checkout, rollback, login/origin, A/B session body binding and stale CAS, no runtime/profile/indexing side effects. Further browser actual/preview dispatch tests follow as runtime reader lands. Full backend compile still being exercised by owners, do not confuse these targeted results with full acceptance.
- Parent removed `compatibilityRuntimeRegistry` from gateway/server.ts. Creating sessions and inspecting native history now consult only the explicitly supplied registry. Runtime owner removes its separate fallback in session-router.ts. No silently created adapter/provider is allowed for a disabled or absent runtime plugin.

These notes complement ownership.md and the actual contract-*.md files. Product decisions and AP11/AP19 gates remain in the owning plan.

## Contract reconciliation

- Core v1 is authoritative: qualified contribution/view IDs use `pluginId/localId`; selections snapshot all local IDs; config targets are `PluginConfigurationTarget`; persisted/browser tabsets use `PluginSessionTabset` and `PluginTabInstance` exactly.
- The initial Management handoff predates core and used `id`, `stateVersion`, `{scope,id}` shorthand. Reconcile these to core's `instanceId`, `stateSchemaVersion`, qualified `viewId`, explicit pluginRevision and discriminated configuration targets. Do not expose a second tab/config DTO to Browser/Runtime.
- The actual operator dispatcher is `src/cli.ts`; `src/bin/pibo.ts` is only the executable entry. Both are orchestrator-owned, along with `src/gateway/server.ts`, `src/gateway/web.ts`, `src/apps/chat/web-app.ts`, `src/index.ts`, build/package metadata, docs and integration fixtures.
- Installed SDK subpaths will be `@pasko70/pibo/plugin-sdk` (browser-safe DTOs/Scope) and `@pasko70/pibo/plugin-host` (backend Host). Do not import the legacy broad types.ts into browser code.

## Baseline evidence available

- Base cac4dcd03945b9754db7be9ab2ab4324f10c335c, fresh upstream/dev.
- `npm run build` passed before code changes, then 58 focused baseline regressions passed.
- `test/fixtures/plugin-system/legacy-builtin-catalog.json` records the actual default registry: 17 native tools, 11 skills, two context files, four old capability packages, three runtimes, four installed CLI context snippets. This is not a full custom-agent selection fixture.
- Existing public OMP plugin identity is `pibo.orp` with profile `orp`; filenames do not imply a different plugin ID. Preserve known persisted identity unless an explicit migration documents it.
- Pibo Gateway send and generated Run/Goal/Subagent tools are not all in the default native-tools list. Inventory their actual callable/yielded sets separately.
- Docs validation now has an isolated Git object/index mirror inside the worker. The real worktree Git is still solely orchestrator-owned on the host. The mirror must be refreshed at later commits before exact traceability checks.

## Integration risks requiring behavioral evidence

1. Registry fallback instances in `src/gateway/server.ts` and `src/core/session-router.ts` cannot silently activate disabled runtime providers. All active domain indexes must be projections of the one host.
2. A plugin setup module must defer adapter-private imports until the selected contribution's runtime requirements have passed. Test a mixed portable/Pi fixture whose Pi module has an observable import side effect.
3. A mere `assertCanActivate` read followed later by async runtime setup is insufficient for the retirement race. Admission must reserve/persist the generation against installation state before yielding, or hold an equivalent lifecycle lease. Uninstall's persisted retirement gate and complete consumer revalidation must observe that reservation.
4. Build Context GET must not create a runtime, connect MCP, call tool factories or execute hooks. Existing inspection had these side effects. Actual recorded snapshots and separate pure preview need distinct IDs and evidence.
5. Pi executable discovery includes model-catalog/bootstrap and package-manager paths, not just session open. Negative fixtures exercise them all while preserving independent skills/context and explicit plugin factories.
6. Consumer analysis must enumerate profiles, persisted generations, active sessions/runs and dependent plugins. Missing legacy snapshots are unknown, never assumed unaffected. Do not reuse Agent-delete or Session-delete services for plugin removal.
7. Inactive/removed browser modules preserve TabInstance state and readable artifacts. Deep links select the encoded session first; late effects retain original session/tab/config scope. Read-only recovery must not activate a session.

## AP11 integration packet

The first installed feature is pibo.web-annotations, using ordinary content-addressed source/manager/host loading and prebuilt browser assets. Backend tools, API, skill, session tab, settings/context subviews and a terminal envelope share its plugin identity. Old capability-package registration disappears after the new path is connected.

The package must be inspectable without import and runnable outside the repository. Its browser module has a declared shared React/SDK contract. No App.tsx feature case, special catalog branch or independent Build Context node list is acceptable.

Local acceptance uses two sessions of one agent plus a differently configured agent. Create/read an annotation through the ordinary authenticated path, inspect it in tab and terminal, change settings within A, compare A's immutable actual build to its preview, switch A→B→A, reload, and test missing-renderer fallback/reinstall with data retained. Use headful Browser Use at 1440×1000 and 390×844 plus CDP console/network evidence. Pibo2 receives the exact committed candidate after local acceptance.

Only the orchestrator marks AP11 accepted and releases broad AP12–14 extraction. Until then, preparation, manifests/contracts and isolated fixtures may proceed, but no claim of complete product migration is warranted.
